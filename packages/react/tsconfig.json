{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    // Node 전역을 끌어오지 않는다. 이 패키지도 브라우저 밖 API를 알 필요가 없다.
    "types": []
  },
  "include": ["src/**/*"]
}
