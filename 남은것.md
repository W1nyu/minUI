# GitHub Pages 공개 데모 기준 — 남은 개발 우선순위

작성일: 2026-08-27  
범위: `https://w1nyu.github.io/minUI/`에서 심사자·테스터가 실제로 보게 되는 정적 배포.  
이 문서는 **사용자 테스트/참가자 모집/사용성 수치 수집은 다루지 않는다.**

## 현재 공개 배포에서 확인한 상태

- 루트(`/minUI/`)는 신한은행·KB증권·하나은행 이식 데모를 쉬운 모드로 연다.
- 은행 시연(`/minUI/bank/`)은 백엔드 없이 탭 전용 가상 원장을 사용한다.
- 은행 시연에는 “가상 오픈뱅킹 시연” 고지, 실제 계좌/토큰/마이데이터 미연결 고지, 화면 도움 설정 동의가 보인다.
- 이체 화면은 수취인·금액을 사람이 확인해야 하며, `보내기` 전에는 가상 원장을 바꾸지 않는다.
- 위 초기 화면과 이체 확인 화면에서 브라우저 콘솔 오류/경고는 없었다.

중요한 배포 조건:

- GitHub Pages는 정적 호스팅이다. 기본 공개 배포에는 Spring API, `/api/assist`, `/api/match`가 없다.
- 따라서 공개 사이트의 기본 가치는 **로컬 검색·정적 뜻풀이·다음 단계 안내·가상 원장 시연**이다.
- 원격 의미 검색은 품질/지연 측정에서 채택되지 않아 기본 비활성화 상태다. 이를 다시 켜거나 모델을 교체하는 일은 현재 우선순위가 아니다.

---

## P0 — 공개 데모의 첫 60초를 하나의 시나리오로 만들기

### 문제

루트는 3개 금융사 이식 데모부터 보여 주고, 가상 이체 시연은 아래쪽의 `미니은행에서 실제로 이체해 보기 →` 링크로 들어간다. 공모전 심사자는 MinUI의 핵심인 **안전한 음성/검색 보조 + 실제로 변화하는 가상 이체**를 바로 발견하지 못할 수 있다.

### 개발할 것

루트 첫 화면에 다음 중 하나를 추가한다.

1. 가장 눈에 띄는 `공모전 시연 시작` CTA → `/minUI/bank/`
2. 3단계 미리보기: “말로 찾기 → 사람이 확인 → 가상 잔액 갱신”
3. 이식 데모와 은행 시연의 목적을 분리한 선택 카드

문구는 “실제 이체”가 아니라 반드시 **“가상 이체/Mock 시연”**이라고 쓴다.

### 완료 기준

- 첫 화면에서 한 번의 선택으로 은행 시연에 들어갈 수 있다.
- 가상 계좌·Mock·사람 최종 확인이라는 안전 경계가 시연 시작 전과 이체 화면에서 모두 보인다.
- `/minUI/`와 `/minUI/bank/`의 링크가 GitHub Pages의 `/minUI/` base path에서도 깨지지 않는다.

### 진행 (2026-08-27) — 끝

개발할 것 1·2를 함께 했다. 루트 맨 위에 `공모전 시연` 띠를 두고
**말로 찾고 → 사람이 확인하고 → 가상 잔액이 바뀝니다** 3단계를 적은 뒤
`가상 이체 시연 시작 →` CTA 하나를 놓았다 (`demos/src/App.tsx`).

문구를 고쳤다. 전에는 **"미니은행에서 실제로 이체해 보기"**였는데 움직이는 것은
가상 원장이다. 시연 범위를 잘못 전달하는 문구가 첫 화면에 있으면 뒤에 붙인 고지가
무색해진다.

base path는 `routePath.ts`가 `import.meta.env.BASE_URL`로 만든다 — 주소를 손으로
쓰면 로컬에서는 되고 `/minUI/`에서만 깨진다. 스모크 ②가 CTA의 `href`를 배포 기준
경로와 대조한다.

---

## P0 — 정적 배포 E2E 스모크 테스트를 자동화하기

### 문제

현재 Vitest는 강하지만, 로컬 소스 중심이다. 공개 GitHub Pages에서만 생기는 base path, 정적 자산, API 부재, 브라우저 저장소 문제를 CI가 잡아 주지 않는다.

### 개발할 것

배포 URL을 대상으로 하는 가벼운 Playwright E2E 스모크 테스트를 만든다. 실제 금융망과 통신하지 않으며, 가상 원장만 다룬다.

필수 시나리오:

1. `/minUI/`가 로드되고 금융사 전환과 쉬운 모드가 보인다.
2. 루트에서 `/minUI/bank/`로 이동할 수 있다.
3. `/minUI/bank/`에서 Mock 고지와 적응 UI 동의 선택지가 보인다.
4. 이체 화면에서 수취인·금액 없이 `보내기`를 누르면 전송되지 않고 안내가 보인다.
5. 테스트 전용 새 브라우저 문맥에서 가상 이체를 완료하면 잔액/거래 내역이 바뀌고, 새 문맥에서는 초기값으로 돌아온다.
6. 각 시나리오에서 console error와 실패한 정적 자산 요청이 없다.

배포 직후 실행하되, GitHub Pages 반영 지연은 재시도 횟수를 제한해 다룬다. 무한 대기나 임의 URL 추측은 하지 않는다.

### 완료 기준

- GitHub Pages URL을 직접 대상으로 한 스모크 테스트가 CI 또는 배포 후 수동 명령으로 재현된다.
- 테스트는 실제 OAuth 권한, 실제 계좌, 실제 API 키를 요구하지 않는다.
- 실패 시 “어느 공개 URL·어느 시나리오·어느 자산”인지 바로 알 수 있다.

### 진행 (2026-08-27) — 끝

`services/smoke`. 여섯 시나리오 전부 통과한다 (`pnpm --filter @minui/smoke smoke`).

**만드는 동안 스모크 자신의 거짓말을 둘 잡았다.** `locator.isVisible()`은 기다리지 않고
그 순간을 답해서, React가 그리기 전에 물으면 언제나 거짓이었다 — 여섯 중 다섯이 헛되이
빨간불이었다. `waitFor`로 감싼 `seen()`으로 바꿨다. 또 하나는 성공을 실패로 읽은 것 —
`계좌 이체` 대화상자를 보고 있었는데 성공하면 `이체 완료`로 넘어간다.

실패 메시지는 주소·시나리오·자산을 다 적는다. `/api/*` 부재로 나는 오류는 **설계된
것**이라 세지 않는다. 이것까지 빨갛게 만들면 아무도 스모크를 안 본다.

**CI에는 안 물렸다.** 완료 기준이 "CI 또는 배포 후 수동 명령"이라 조건은 채웠지만,
자동으로 도는 것은 없고 손으로 돌릴 때만 확인된다. 배포마다 크롬을 띄우면 워크플로가
2분쯤 길어지고 Pages 반영 지연으로 헛빨간불이 난다 — **나중에 판단할 것.**

---

## P1 — 공개 배포에서 가능한 AI와 불가능한 AI를 화면/문서에 맞추기

### 문제

정적 배포에서는 원격 도우미와 원격 의미 검색이 기본적으로 없다. 그러나 일부 소개 문구는 원격 AI 보강이 즉시 동작하는 것처럼 읽힐 수 있다. 또한 구조화된 코파일럿 제안의 안전 검증(`validateProposal`)은 구현되어 있지만, 공개 UI에서 외부 모델의 구조화된 응답을 보여 주는 흐름은 아직 핵심 시연이 아니다.

### 개발할 것

둘 중 하나를 명확히 선택한다.

#### 선택 A — 정적·안전 데모를 유지한다 (권장)

- 공개 UI에 “이 데모는 로컬 검색과 미리 준비된 설명으로 동작합니다”를 짧게 알린다.
- AI 코파일럿은 `다음 단계 안내`, `용어 풀이`, `모델 응답을 검증할 수 있는 안전 경계`로 설명한다.
- 원격 AI가 없는 것을 오류나 빈 기능처럼 보이게 하지 않는다.

#### 선택 B — 원격 코파일럿을 공개 시연한다

- 별도 서버리스 경계에서 제한된 JSON만 반환하게 한다.
- 숫자·계좌번호·금액·수취인·음성 원본은 요청 전에 차단한다.
- 응답은 반드시 `validateProposal`/카탈로그/위험도 규칙을 통과시킨 뒤 후보 카드로만 보여 준다.
- AI 실패·429·시간 초과 때 로컬 검색과 전체 메뉴로 즉시 돌아가는 E2E 테스트를 추가한다.

선택 B는 “AI가 더 있어 보인다”는 이유만으로 시작하지 않는다. 키 관리, 호출 제한, 개인정보 고지, 실패 UX까지 공개 배포에서 검증할 수 있을 때만 한다.

### 완료 기준

- README, 공개 화면, 기획안의 AI 설명이 실제 GitHub Pages 동작과 일치한다.
- 어떤 경우에도 AI가 고위험 메뉴를 자동으로 열거나 가상 원장을 직접 호출하지 않는다.

### 진행 (2026-08-27) — 끝. **선택 A**

`README.md`에 공개 배포와 로컬을 나란히 놓은 표를 뒀다 — 도우미·원격 의미 검색이
Pages에서 각각 어떻게 되는지. 첫 화면에도 적었다: *검색·뜻풀이·다음 단계 안내는
이 기기 안에서 돕니다.*

**핵심은 "없는 것을 안 보이게 만든" 쪽이다.** 중계기 주소가 없으면 `assist`를 아예
안 넘긴다. `enabled: false`로 두는 것과 다르다 — 후자면 저신뢰 검색마다 404를 한 번
받고 버리고, 화면에는 "묻는 중"이 잠깐 떴다 사라진다. 안 넘기면 그 상태 자체가 안
생기고 되묻기가 그대로 답이 된다. 프로덕션 번들에서 호출 경로도 사라진다.

고위험 자동 실행은 `NEVER_AUTO_OPEN`과 `requiresConfirm`이 막는다. 코파일럿 제안은
`validateProposal`을 지나야 하고, 모델은 위험도를 **올릴 수만** 있다(`combineRisk`).

---

## P1 — 접근성 검증을 실제 배포/실제 브라우저로 확장하기

### 문제

컴포넌트 테스트의 axe 검사에서 색 대비와 터치 대상 크기 규칙이 일부 비활성화돼 있다. 이 프로젝트의 핵심 가치가 접근성인 만큼, 이 두 항목을 “테스트 환경 한계”로 오래 남겨 두면 약점이 된다.

### 개발할 것

GitHub Pages를 대상으로 아래 체크를 자동화하거나 재현 가능한 수동 체크리스트로 만든다.

- 360px 폭, 태블릿, 데스크톱에서 가로 스크롤/잘린 버튼이 없는지
- 200% 확대에서 핵심 경로(모드 전환, 검색, 이체 확인)가 가능한지
- 키보드만으로 초점 순서·모달 닫기·선택·전송 직전 확인이 가능한지
- 실제 렌더된 텍스트/배경 대비와 최소 터치 영역을 측정하는지
- Web Speech 미지원/권한 거절 상태에서 텍스트 검색이 같은 기능에 도달하는지

색상이나 카드 밀도를 바꾸는 작업은 이 검증을 통과한 뒤에만 한다. 특히 큰 카드·큰 글자·확인 문구를 시각적으로 더 "세련되게" 만들려다 접근성을 줄이면 안 된다.

### 완료 기준

- 색 대비와 터치 대상 크기를 제외하지 않는 검증 근거가 생긴다.
- 공개 URL의 모바일·키보드·확대 테스트 결과가 문서화된다.

### 진행 (2026-08-27) — 끝

`services/smoke/src/a11y.ts`. 여섯 검사 전부 통과하고, **결과는
`docs/기획안.md` §11.3에 표로 남겼다.**

360·768·1280px / 200% 확대 / 키보드만 / Web Speech 없는 브라우저 / 터치 88×88 /
axe를 `color-contrast`·`target-size`를 **켜고** wcag2a~21aa로.

**켜고 재니 진짜 결함이 하나 나왔다.** 적응 UI 동의 버튼이 40px이었다. 동의를 묻는
버튼이 작으면 손떨림이 있는 사람은 **거절하려다 허락하게 된다.** jsdom에서 못 잰다고
꺼 둔 채로 뒀으면 지금도 배포에 있었을 것이다.

**검사 자신의 결함도 하나 나왔다.** `min-height: 88px`이 실측 `87.99996948242188`인데
표시만 반올림해 **"88×88은 88보다 작다"**는 모순된 실패를 냈다. 서브픽셀 0.5px을
봐주되 진짜 부족은 여전히 걸리게 고쳤다.

---

## P1 — Mock의 “부분 재현”을 발표 가능한 계약으로 고정하기

### 문제

가상 오픈뱅킹 Mock은 안전한 시연 목적에 맞지만, 현재 필드 대조 테스트는 선언 25개 중 실제 공개 명세와 대조 기록이 0개인 상태다. 즉 테스트는 형식 목록을 보관하지만 아직 계약 근거가 충분하지 않다.

### 개발할 것

- 잔액조회와 핀테크이용번호 기반 입금이체에 쓰는 필드만 대상으로 한다.
- 금융결제원 공개 문서의 확인 날짜/필드명/필수 여부/Mock 처리 방식을 fixture 또는 표에 남긴다.
- 성공뿐 아니라 잘못된 가상 토큰, 존재하지 않는 핀테크이용번호, 잔액 부족, 같은 `bank_tran_id` 재시도를 명시적으로 대조한다.
- “재현하지 않는 OAuth/인증서/실계좌/수수료 정산”을 테스트와 발표 화면에서 분명히 유지한다.

### 완료 기준

- Mock이 재현하는 부분과 재현하지 않는 부분을 필드 단위로 설명할 수 있다.
- Spring Mock 통합 테스트와 정적 `sessionStorage` Mock의 핵심 성공/실패 결과가 같다는 테스트가 있다.

### 진행 (2026-08-27)

**오류 계약 대조는 끝났다.** `shared/contracts/openbanking-cases.json`에 여섯 경우를 적고,
양쪽이 **같은 파일을 읽는다** — `frontend/test/openBankingErrors.test.ts`(8건)와
`backend/.../OpenBankingMockApiTest.java`(6건).

맞추는 것은 응답 모양이 아니라 **결과**다. 실패는 KFTC 모양으로 돌려주지 않기 때문이다 —
Spring은 `422/409 + {message}`, 정적 Mock은 예외를 던진다. 그렇게 둔 것이 의도라는 근거가
`ApiExceptionHandler`에 있다("실제 금융결제원 응답으로 오인하지 않도록"). 그래서 재는 것은
**거절하는가**와 **같은 이유를 말하는가**다.

대조가 실제로 도는지 확인했다. 표의 `says`를 일부러 `잔액` → `한도초과`로 바꿔 보니
정적 쪽은 걸렸는데 Spring 쪽은 `BUILD SUCCESSFUL in 1s`로 지나갔다 — 표가 Gradle의 선언된
입력이 아니어서 up-to-date로 건너뛴 것이다. `inputs.file(...)`로 선언한 뒤 다시 재니 양쪽 다
걸렸다. **이 확인을 안 했으면 Spring 쪽 대조는 있으나 마나였다.**

**필드 대조도 끝났다 (2026-08-28).** 규격서가 로그인 없이 공개돼 있었다 —
[developers.kftc.or.kr/dev/openapi/open-banking/deposit](https://developers.kftc.or.kr/dev/openapi/open-banking/deposit).
`official` 칸을 그 표에서 옮겼고, 출처와 확인 날짜를 픽스처에 함께 적었다.

> **규격 29개 중 25개를 낸다. 안 내는 4개는 이유를 적었다.**

안 내는 넷은 `wd_bank_code_sub` · `bank_code_sub` · `savings_bank_name` · `cms_num`이다.
표에서 **지우지 않고 줄로 남겼다** — 지우면 "25개 중 25개"로 보인다. 기관 서브코드와
CMS 번호는 실제 참가기관·정산 체계의 값이라, 지어내면 규격을 흉내 낸 것이 아니라 없는
기관을 만드는 것이 된다.

**대조가 진짜 위반을 하나 잡았다.** `bank_tran_id`가 `AN(20)`(영문·숫자만)인데 앱이
넘기는 `crypto.randomUUID()`의 하이픈이 그대로 실려 나갔다. 정적 Mock과 HTTP 어댑터
**양쪽 다** 그랬다 — 우리 규칙(`atMost20`)만 보던 동안은 통과했다. 영숫자만 남겨 자르도록
고쳤고, 멱등 판정은 원본 키로 하므로 재시도 동작은 그대로다.

테스트가 이제 세 가지를 잰다: 규격 필수 필드를 **내는가**, 그 값이 규격의 타입(길이)
**안에 드는가**, 안 내는 것에 **이유가 적혀 있는가**. `rsp_code`도 규격이 `AN(5)`로
확인해 줬다 — 4자로 기억했다가 걸렸던 그 값이다.

---

## P2 — 성능 예산을 정하고, 측정 뒤에만 번들 분리하기

### 문제

데모 빌드는 큰 청크 경고가 있다. 하지만 현재는 공개 배포의 실제 로드 시간/상호작용 지연 예산이 정해지지 않아, 무작정 dynamic import를 추가하면 시연 첫 동작만 더 늦어질 수 있다.

### 개발할 것

- GitHub Pages의 모바일 환경에서 초기 JS 크기, LCP, 첫 상호작용까지의 시간, 검색 시 반응 시간을 측정한다.
- 예산을 정한다. 예: 첫 화면·모드 전환·말로 찾기 버튼은 시연 중 즉시 반응해야 한다.
- 예산을 넘길 때만 Studio, 사이트별 카탈로그, 드문 관리 화면 등을 지연 로드한다.
- 분리 전후 수치와 실패 시 폴백을 문서에 남긴다.

### 완료 기준

- “청크가 크다”가 아니라 측정값과 예산을 근거로 최적화 여부를 결정한다.
- 지연 로딩 후에도 공개 URL의 직접 진입과 새로고침이 깨지지 않는다.

### 진행 (2026-08-27) — **재 봤고, 쪼개지 않기로 한다**

`services/smoke/src/perf.ts`를 만들어 공개 배포를 모바일 조건에서 쟀다
(CPU 4배 감속 · 1.6Mbps · 지연 150ms — Lighthouse 모바일과 같은 방식).

| | JS 전송 | LCP | 주 버튼까지 |
|---|---|---|---|
| 루트 | **200KB** / 313KB | 1,800ms / 3,000ms | 1,820ms / 4,000ms |
| 미니은행 | **99KB** / 146KB | 1,388ms / 2,500ms | 1,409ms / 3,000ms |

손이 닿은 뒤: 온보딩 2문항 → 홈 **906ms**, 전체 메뉴 → 목록 163ms, 말로 찾기 → 시트 67ms
(예산 각 1,000ms).

**여덟 칸 전부 예산 안이다.** 빌드가 경고하던 청크는 원본 1.17MB지만 gzip으로
**200KB**가 도착한다 — 파일 크기와 전송량을 혼동해 쪼갰으면 왕복만 늘려 첫 화면을
더 늦출 뻔했다. 그래서 **지금은 분리하지 않는다.**

한 칸만 여유가 좁다. **온보딩 906ms**는 예산 1,000ms에 붙어 있고, 처음 오는 사람이
반드시 지나는 길이다. 여기가 넘으면 그때 그 칸만 본다.

예산은 `perf.ts`의 `TARGETS`에 근거와 함께 박혀 있고, 넘으면 종료 코드가 1이다.

```powershell
pnpm --filter @minui/smoke perf
```

---

## P2 — 이식 데모의 카탈로그 신뢰도를 운영 가능하게 만들기

### 문제

이식형 UI의 핵심 자산은 카탈로그다. 실제 금융사 메뉴는 로그인, robots, 동적 렌더링, URL 변경 때문에 시간이 지나면 오래된 메뉴를 보여 줄 수 있다.

### 개발할 것

- 각 배포 카탈로그에 수집 날짜, 출처 URL, 메뉴 수, 마지막 검증 결과를 표시하거나 메타데이터로 남긴다.
- 수집/재빌드 때 메뉴 수 급감, ID 표류, 사람이 쓴 override 고아화를 CI에서 실패 또는 명확한 경고로 처리한다.
- 공개 화면에는 “이식 검증용 수집본”임을 유지하고, 실제 금융사 서비스의 최신 메뉴라고 주장하지 않는다.

### 완료 기준

- 카탈로그가 언제·어디서 왔고 얼마나 신뢰할 수 있는지 확인할 수 있다.
- 금융사 사이트 변경이 조용히 검색 품질을 무너뜨리지 않는다.

### 진행 (2026-08-27) — **기록하고, 급변을 막는다**

`demos/src/catalogs/provenance.json`을 빌더가 굽는다. 사이트마다 host·수집 시각·
메뉴 수·원본 항목 수·고아 override·재연결 수. **사람이 따로 적지 않는다** — 적게 하면
카탈로그만 다시 굽고 날짜는 옛날 것이 남는다.

`tools/src/provenance.ts`의 `checkDrift()`가 지난 기록과 견준다.

| | 처리 |
|---|---|
| 메뉴가 20% 넘게 줄었다 | **실패.** 사이트가 정말 줄인 것보다 로그인 풀림·동적 렌더링·선택자 변경이 훨씬 잦다 |
| 사이트가 통째로 사라졌다 | **실패** |
| `capturedAt`을 못 읽는다 | **실패.** "언제 왔는지 안다"는 주장 자체가 무너진다 |
| 붙지 못한 override가 있다 | 경고. 사람이 봐야 하지만 빌드를 세울 일은 아니다 |
| 수집한 지 180일 넘었다 | 경고 |

**막힐 때 아무것도 쓰지 않는다.** 처음엔 카탈로그를 먼저 쓰고 뒤에서 검사했는데,
그러면 망가진 카탈로그가 이미 디스크에 앉은 뒤라 "덮어쓰지 않았다"는 메시지가 절반만
참이었다. 기준을 흔들어 확인하고 순서를 뒤집었다 — 지금은 `shinhan.json`을 비워 두고
깨진 기준으로 돌리면 **비워 둔 그대로 남는다.**

화면에는 `2026. 8. 10. 수집본 (이식 검증용)`이 출처 옆에 붙는다. 날짜와 "수집본"이
둘 다 있어야 한다 — 하나만 있으면 그 금융사의 **현재 메뉴**로 읽힌다.

테스트 19건(`tools/test/provenance.test.ts`). 커밋된 기록이 커밋된 카탈로그와 실제로
맞는지도 잰다 — 기록과 카탈로그가 따로 놀면 다음 빌드가 낡은 기준과 견줘 감시가 헛돈다.

---

## 지금 하지 않을 것

- 원격 의미 검색 모델/재순위 모델을 다시 교체하거나 기본 활성화하기
- 실제 은행 API, 실제 OAuth, 실제 고객 계좌, 마이데이터 연결하기
- AI가 금액·수취인·계좌를 받아 자동 이체하게 만들기
- 사용자 검증 근거 없이 카드 순서·적응 임계값·검색 가중치를 임의로 튜닝하기

## 권장 작업 순서

1. P0 공개 진입 동선 개선
2. P0 배포 URL E2E 스모크 테스트
3. P1 정적 배포의 AI 설명 정합성 결정
4. P1 실제 브라우저 접근성 검증
5. P1 Mock 필드/오류 계약 대조
6. P2 성능 예산과 카탈로그 운영성

## 현재 기계 검증 상태

2026-08-27에 확인:

```powershell
pnpm typecheck
pnpm test   # 57개 파일, 810개 테스트 통과
```

새 작업도 관련 단위 테스트를 먼저 추가하고, 완료 전에는 전체 타입 검사·테스트·공개 GitHub Pages 스모크 테스트를 모두 실행한다.
