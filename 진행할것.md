# Claude 작업 인계 — 다음 진행할 것

작성일: 2026-08-26  
현재 작업 트리는 **커밋되지 않은 변경이 있는 상태**다. 기존 변경을 덮어쓰거나 되돌리지 말고,
아래 상태를 기준으로 이어서 작업한다.

## 지금 구현된 것

### M15 — 공모전용 가상 오픈뱅킹 시연

- 실제 은행 송금 API·마이데이터·실제 OAuth 토큰은 연결하지 않는다.
- 테스트 계좌 6개(주거래, 적금, 관리사무소, 김미영, 박정호, 김영수 삼촌)와 가상 원장을 만들었다.
- 사용자가 이체 화면에서 수취인·금액을 확인하고 **`보내기`를 탭한 뒤에만** 출금/입금이 실행된다.
- 음성·검색·sLLM은 메뉴/수취인/금액의 **제안**까지만 한다. 이체 API를 직접 호출하면 안 된다.
- 정적 배포는 `sessionStorage` 기반 Mock, 로컬 Spring 서버는 PostgreSQL 기반 Mock을 쓴다.
- 금융결제원 공개 API 흐름 중 잔액조회·핀테크이용번호 기반 입금이체의 URL/JSON 필드 일부를 재현했지만,
  **상용 규격 전체 호환성이나 실제 연동을 주장하지 않는다.**

주요 파일:

- `frontend/src/api/openBankingMock.ts`
- `frontend/src/api/httpApi.ts`
- `backend/src/main/java/com/minui/bank/web/OpenBankingMockController.java`
- `frontend/src/screens/TransferScreen.tsx`
- `frontend/test/openBankingMock.test.ts`
- `backend/src/test/java/com/minui/bank/OpenBankingMockApiTest.java`

### M16 — 동의형 적응 UI

- 사용자가 허용한 경우에만 탭 안에서 긴 누름, 빠른 되돌아감, 음성 대기 시간을 **숫자 합계**로 집계한다.
- 도움 정도는 `완전 단순형 / 하이브리드 안내형 / 일반 단순형` 3단계다.
- 행동 데이터는 `sessionStorage`에만 있고, 탭 종료 또는 “도움 기록 지우기”로 삭제된다.
- 메뉴 ID·좌표·원문 음성·인식 문장·계좌번호·금액·수취인은 저장하거나 전송하면 안 된다.
- 이 기능은 “디지털 불안도”를 진단하거나 점수화하는 기능이 아니다. 화면에는 “화면 도움 정도”만 보인다.

주요 파일:

- `frontend/src/adaptation/AdaptiveSupport.tsx`
- `frontend/src/modes/MinUIShell.tsx`
- `packages/react/src/MinUIHome.tsx`
- `packages/react/src/VoiceSearchSheet.tsx`
- `frontend/test/adaptiveSupport.test.tsx`

### 원격 AI 개인정보 경계

- `/api/assist`에는 숫자·송금·계좌·수취인 표현이 든 발화를 보내지 않는다.
- 그런 발화는 로컬 검색/확인 경로로 끝나야 한다.
- API 키는 브라우저 번들, 저장소, 오류 메시지에 넣으면 안 된다.

주요 파일:

- `shared/host-ai/assist.ts`
- `frontend/test/assistPrivacy.test.ts`

## 우선순위별 다음 작업

> §4(M11 의미 검색)는 **할 일이 아니라 결론**이다 — 2026-08-26에 재 보고 접었다.
> 검색 정확도를 올리는 제안을 하기 전에 그 절을 먼저 읽을 것.

### 1. 사용자 테스트 실행 — 가장 중요 (M10 + M16)

코드만으로 증명할 수 없는 항목이다. 참가자를 실제로 모집해 아래를 측정한다.

- 60대 이상 5명, 디지털 사용 빈도가 낮은 40~50대 3명 정도를 목표로 한다.
- 쉬운 UI와 일반 UI를 비교한다.
- 완료 시간, 완료율, 과제당 오탐색/되돌아감 횟수를 기록한다.
- 적응 UI는 동의 여부, 화면 변화 수용도, 도움이 됐는지를 인터뷰한다.
- 결과가 좋지 않아도 수치를 맞추거나 누락하지 말고 그대로 보고한다.

절차와 기록 양식은 `docs/사용자테스트-키트.md`를 따른다. 세션 파일을 넣은 뒤:

```powershell
pnpm --filter tools report:usertest
```

을 실행해 결과를 낸다. 실제 참가자 데이터는 저장소에 커밋하지 않는다.

### 2. 가상 이체 시연 리허설 및 백엔드 통합 테스트

발표용 고정 시나리오를 반복 점검한다.

```text
“삼촌에게 3만 원 보내기”
→ 이체 후보 선택
→ 수취인·금액을 사람이 확인
→ 보내기 탭
→ Mock JSON 응답
→ 가상 잔액과 거래 내역 변화 확인
```

- 브라우저 정적 Mock과 로컬 Spring Mock 모두에서 확인한다.
- Docker Desktop을 쓸 수 있는 환경에서는 `OpenBankingMockApiTest`를 실제 실행한다.

```powershell
docker compose -f backend/compose.yaml up -d
Set-Location backend
.\gradlew.bat test --tests com.minui.bank.OpenBankingMockApiTest
```

현재 Java 컴파일 및 테스트 클래스 생성은 통과했지만, Testcontainers 통합 테스트는 Docker가
없는 환경에서는 실행할 수 없다.

### 3. M12 — AI 금융 접근성 코파일럿을 좁은 역할로 완성

다음 세 역할에 한정해 설계/구현/측정한다.

- 어려운 금융 용어를 쉬운 말로 설명
- 사용자가 현재 화면에서 할 수 있는 다음 단계를 안내
- 발화를 검증 가능한 메뉴 ID·제한된 의도 스키마로 제안

금지:

- 존재하지 않는 메뉴를 만드는 것
- 위험도를 낮추는 것
- 고위험 행동을 자동 실행하는 것
- 수취인·금액·계좌정보를 원격 모델에 보내는 것

AI가 꺼지거나 실패해도 로컬 검색, 전체 메뉴, 사람이 읽을 수 있는 안내로 끝까지 진행돼야 한다.

### 4. M11 — 끝났다. **로컬 검색이 기본값이다** (2026-08-26 결정)

이 절은 할 일이 아니라 **결론**이다. 전에 여기 적혀 있던 "공식 금융 안내문을 카탈로그의
근거 신호로 쓰는 방법을 구현한다"는 **재 보고 접었다.** 다시 시작하기 전에 아래 수치를
먼저 볼 것. 전체 기록은 `docs/superpowers/plans/2026-08-24-official-documents.md`의
「Task 1 결과」에 있다.

#### 무엇이 결정됐나

원격 의미 검색은 사전 등록 게이트를 못 넘었다 — `semantic-focus` **12.4%**.
값을 올리려고 네 지렛대를 당겼고 **넷 다 측정으로 실패했다.**

| 시도 | 결과 |
|---|---|
| 하이브리드 (dense+sparse) | 진단이 틀렸다. `recall@20` 50.9% — 회수기가 병목이 아니다 |
| 교차 인코더 재순위 | **12.4% → 5.7%** (더 나쁨). p95 5,915ms = 게이트 1,200ms의 5배 |
| 문서에 뜻풀이 넣기 | 순환. blind +31.7%p인데 **사람이 쓴 질의 −6.7%p** |
| 공식 문서 수집 | **재료가 판정 세트에 안 닿는다** (아래) |

#### 공식 문서 수집을 접은 이유 — 구조적이다

두 사이트를 서로 다른 방법으로 재서 같은 답이 나왔다.

```
하나은행  메뉴 링크 652개 중 502개(77%)가 .do — robots가 막고, 애초에 거래 화면이다
          legacy 15건 중 문서가 닿는 것 1건
KB증권    정답 메뉴는 전부 URL이 있는데, 표본 9개 중 6개가 로그인 페이지(본문 482자)
KB국민    robots가 `User-agent: * / Disallow: /` — 전면 금지
신한·미래에셋  메뉴 링크가 `javascript:void(null)` — 붙일 URL이 아예 없다
```

원인은 사이트 하나의 사정이 아니다. 평가 세트의 고유 정답 메뉴 118개를 갈라 보면
**거래/신청 46 · 조회 19 · 상품 8**이고, **은행은 가입을 유도하는 상품에만 공개 설명을
두고 로그인 뒤 거래 화면에는 두지 않는다.** 그런데 사용자가 못 찾는 것은 후자다.
공식 문서는 있는 곳에만 있고, 그곳은 우리가 필요한 곳이 아니다.

#### 그래서 지금 상태

- **로컬 검색이 기본값이다.** `packages/core/src/config.ts`의 `neural.enabled`가 `false`
- 코드는 **한 줄도 안 지웠다** — `services/matcher`, `matchRoute`, 평가 세트 558건,
  유도한 값(0.740/0.825/1.0)이 그대로 있다. 문서가 풍부해지면 다시 값을 할 수 있어서다
- 로컬에서 켜 보려면 `MINUI_NEURAL=1 VITE_MINUI_NEURAL=1 pnpm --filter demos dev`

#### 남은 코드 정리 하나 (아직 안 함)

`demos/src/App.tsx`가 아직 `neural: { enabled: true }`에 `retrieve`를 무조건 넘긴다.
정적 배포에는 `/api/match`가 없어 **저신뢰 검색마다 404를 한 번 받고 버린다** —
동작은 옳지만(빈 결과 → 로컬 결과) 쓸데없는 요청이다.

`shared/host-ai/assist.ts`가 쓴 방식을 그대로 쓰면 된다: 플래그가 없으면 `retrieve`를
**아예 안 넘긴다.** 그러면 프로덕션 번들에서 호출 경로가 사라진다. 5줄쯤이고
급하지 않다.

#### 다시 열려면

모델이나 알고리즘을 바꾸는 제안은 **하지 말 것.** 넷 다 그렇게 실패했다.
조건은 하나다 — **문서에 사용자 언어가 생기는 것.** 사람 동의어가 붙은 세트에서는
같은 모델이 62.7%를 낸다. 남은 공급원은 실사용자뿐이고, 그것이 §1(사용자 테스트)이
지금도 최우선인 이유다. 거기서 나온 낱말을 `tools/catalogs/*.overrides.json`에 넣고
`pnpm --filter tools bench:sites`로 전후를 재면 된다.

모델 파일·벡터·API 키는 클라이언트나 `packages/*`에 넣지 않는다.

### 5. 발표 전 품질 점검

- 네트워크/AI 실패 시에도 검색·전체 메뉴·이체 확인이 작동하는지 확인한다.
- Mock이라는 상시 표시가 사라지지 않는지 확인한다.
- `pnpm build`의 데모 번들 크기 경고(현재 큰 청크 약 1.17 MB)를 측정한 뒤 필요하면 동적 import로 분리한다.
  성능 수치 없이 무작정 분리하지 않는다.
- 사용자 테스트 이후에만 M14(키오스크·보호자 시나리오) 확장을 결정한다.

## 절대 지켜야 할 규칙

1. `riskLevel: "high"` 메뉴는 음성·검색·AI에서 자동 실행하지 않는다.
2. 실제 은행 API, 실제 고객 계좌, 실제 OAuth/마이데이터 연결을 추가하지 않는다.
3. AI는 위험도를 낮추거나 안전 확인을 생략할 수 없다.
4. 계좌번호·금액·수취인·음성 원본을 저장하거나 원격 AI로 보내지 않는다.
5. 행동 적응은 사전 동의, 탭 전용 집계, 즉시 삭제, 비진단 원칙을 지킨다.
6. `packages/core`는 의존성 0인 순수 TypeScript 경계를 유지한다.
7. 로직/검색/랭킹을 바꾸면 먼저 실패하는 테스트와 fixture를 추가한다.

상세 규칙은 `AGENTS.md`, 설계·마일스톤·측정 근거는 `docs/기획안.md`가 기준이다.

## 현재 검증 상태

2026-08-27 기준 다음이 통과했다.

```powershell
pnpm typecheck
pnpm test   # 54개 파일, 773개 테스트 통과
pnpm --filter demos build
pnpm --filter frontend build

Set-Location backend
.\gradlew.bat test --tests com.minui.bank.OpenBankingMockApiTest   # Docker 필요
```

`OpenBankingMockApiTest`(Testcontainers)는 **Docker 29.6.1에서 실제로 통과했다.**
전에 "Docker 없는 환경에서는 실행할 수 없다"고 남겨 뒀던 항목이 닫혔다.

변경 후에는 최소한 관련 테스트를 실행하고, 완료 전에는 다시 전체 테스트·타입 검사·빌드를 실행한다.

> **테스트만 돌리고 넘어가면 CI에서 걸린다.** vitest는 `@minui/core`를 **소스로** 별칭 걸고
> (`vitest.config.ts`), `tsc`는 워크스페이스 심링크를 따라 **dist의 `.d.ts`**로 간다.
> 그래서 테스트는 통과하는데 빌드가 깨지는 일이 실제로 있었다. 테스트를 추가한 뒤에는
> CI와 같은 순서를 밟을 것 — 패키지 빌드 → `pnpm test` → demos·frontend 빌드.
