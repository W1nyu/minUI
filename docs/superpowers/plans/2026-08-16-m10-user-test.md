# M10 사용자 테스트 준비 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 참가자 8명을 앉히는 날 그대로 돌아가도록, 진행 키트·음성 주입 손잡이·지표 집계 도구를 만든다.

**Architecture:** 셋은 서로 독립이다. ① `tools`에 순수 함수 둘(봉투 파싱, 집계)과 얇은 CLI 하나 — 기존 `report-metrics.ts`가 비워 둔 세 칸을 채우는 짝이다. ② `frontend/src/instrumentation`에 실제 STT를 **감싸는** provider와 콘솔 손잡이 — `MetricsBridge`와 같은 패턴. ③ `docs/사용자테스트-키트.md` 한 장.

**Tech Stack:** TypeScript · vitest (프로젝트 `tools`·`frontend`) · tsx (스크립트 실행) · React 19

## Global Constraints

- **`packages/*`를 건드리지 않는다.** 이식 계약이 넓어지면 프로젝트 전제가 무너진다 (AGENTS.md 불변 규칙 2)
- **TDD.** 실패하는 테스트 → 최소 구현 → 리팩터. 테스트 없는 로직 변경은 받지 않는다 (AGENTS.md 개발 방식)
- **완료를 주장하기 전에 실제로 명령을 실행하고 그 출력을 근거로 삼는다**
- 주석과 문서는 한국어. 기존 파일들의 밀도를 따른다 — **무엇을 하는가가 아니라 왜 그렇게 했는가**를 적는다
- 재지 못한 것은 **빈칸으로 남긴다.** 스크립트가 만든 숫자를 사람이 재야 하는 자리에 넣지 않는다 (`tools/src/report-metrics.ts` 헤더 주석)
- 표본 8명에 검정력이 없다. **도구가 p값이나 유의성 판정을 계산하지 않는다**
- 새 의존성을 추가하지 않는다. `tools`의 `dependencies`는 `@minui/core` 하나로 둔다

## 파일 구조

| 파일 | 책임 |
|---|---|
| `tools/src/usertest-session.ts` (신규) | 세션 봉투의 형 정의와 파싱·검증. **입출력 없음** |
| `tools/src/usertest-report.ts` (신규) | `Session[]` → `Report`. 순수 계산. **입출력 없음** |
| `tools/src/report-usertest.ts` (신규) | CLI — 디렉터리 읽기와 표 출력만 |
| `tools/test/usertest-session.test.ts` (신규) | 봉투 검증 |
| `tools/test/usertest-report.test.ts` (신규) | 집계 규칙 |
| `tools/sessions/EXAMPLE.json` (신규) | 형식 예시. 실제 기록은 `.gitignore` |
| `tools/package.json` (수정) | `report:usertest` 스크립트 |
| `.gitignore` (수정) | `tools/sessions/` 제외, `EXAMPLE.json`만 남김 |
| `frontend/src/instrumentation/ScriptedOverrideStt.ts` (신규) | 실제 STT를 감싸 최종 전사만 갈아끼운다 |
| `frontend/src/instrumentation/SttBridge.tsx` (신규) | `window.minuiStt` 노출 |
| `frontend/src/main.tsx` (수정) | `?stt=script` 배선 |
| `frontend/test/sttOverride.test.ts` (신규) | 덮어쓰기 규칙 |
| `docs/사용자테스트-키트.md` (신규) | 진행자용 |
| `docs/기획안.md` (수정) | §12.10·§13 |
| `AGENTS.md` (수정) | 명령 목록에 `report:usertest` |

집계를 파싱·계산·출력 셋으로 가른 이유는 **계산에 파일 시스템이 안 붙어야 테스트가 쉬워서**다. `report-metrics.ts`는 셋이 한 파일에 있어 테스트가 없다 — 같은 실수를 되풀이하지 않는다.

---

### Task 1: 세션 봉투 파싱·검증

**Files:**
- Create: `tools/src/usertest-session.ts`
- Test: `tools/test/usertest-session.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `AgeBand`, `Mode`, `F9Outcome`, `M7Repeat`, `TaskRun`, `Session` 타입과 `parseSession(raw: unknown, source: string): Session`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tools/test/usertest-session.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { parseSession } from "../src/usertest-session.js";

/** 정상 봉투 하나. 각 테스트가 여기서 한 필드만 망가뜨린다. */
function envelope(): Record<string, unknown> {
  return {
    participant: "P01",
    ageBand: "60+",
    firstMode: "minui",
    runs: [
      {
        taskId: "T1",
        mode: "minui",
        targetMenuId: "inquiry.balance",
        startedAt: 0,
        endedAt: 12_000,
        elapsedMs: 12_000,
        taps: 1,
        screens: ["inquiry.balance"],
        offTarget: 0,
        completed: true,
      },
    ],
    interventions: { "T2:classic": 2 },
    words: { T2: ["자동으로 나가는 거"] },
    f9: "read-not-tapped",
    m7Repeat: "same",
  };
}

describe("parseSession", () => {
  it("정상 봉투를 그대로 읽는다", () => {
    const session = parseSession(envelope(), "P01.json");

    expect(session.participant).toBe("P01");
    expect(session.ageBand).toBe("60+");
    expect(session.runs[0]?.elapsedMs).toBe(12_000);
    expect(session.interventions["T2:classic"]).toBe(2);
    expect(session.words.T2).toEqual(["자동으로 나가는 거"]);
    expect(session.f9).toBe("read-not-tapped");
  });

  it("F9와 M7 반복은 없어도 된다 — 미수행일 수 있다", () => {
    const raw = envelope();
    delete raw.f9;
    delete raw.m7Repeat;

    const session = parseSession(raw, "P01.json");

    expect(session.f9).toBeUndefined();
    expect(session.m7Repeat).toBeUndefined();
  });

  it("interventions와 words는 없으면 빈 것으로 채운다", () => {
    const raw = envelope();
    delete raw.interventions;
    delete raw.words;

    const session = parseSession(raw, "P01.json");

    expect(session.interventions).toEqual({});
    expect(session.words).toEqual({});
  });

  // 조용히 넘기면 그 참가자가 통째로 표에서 사라지고, 8명 중 7명인 것을 아무도 모른다.
  it("연령대 오타는 파일 이름과 함께 실패한다", () => {
    const raw = { ...envelope(), ageBand: "60대" };

    expect(() => parseSession(raw, "P03.json")).toThrow(/P03\.json.*ageBand/s);
  });

  it("수행의 모드 오타도 잡는다", () => {
    const raw = envelope();
    (raw.runs as Record<string, unknown>[])[0]!.mode = "easy";

    expect(() => parseSession(raw, "P03.json")).toThrow(/P03\.json.*mode/s);
  });

  it("runs가 배열이 아니면 실패한다", () => {
    const raw = { ...envelope(), runs: {} };

    expect(() => parseSession(raw, "P03.json")).toThrow(/P03\.json.*runs/s);
  });

  it("참가자 번호가 없으면 실패한다", () => {
    const raw = envelope();
    delete raw.participant;

    expect(() => parseSession(raw, "P03.json")).toThrow(/P03\.json.*participant/s);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
pnpm vitest run --project tools test/usertest-session.test.ts
```

기대: FAIL — `Failed to resolve import "../src/usertest-session.js"`

- [ ] **Step 3: 최소 구현을 쓴다**

`tools/src/usertest-session.ts`:

```ts
/**
 * 사용자 테스트 세션 봉투 (기획안 §12.10).
 *
 * <p>`TaskRun[]`에는 참가자도, 어느 모드를 먼저 했는지도, 진행자가 몇 번 개입했는지도
 * 없다. 그것들이 없으면 순서 효과를 못 보고 개입이 실린 수행을 못 가른다. 그래서 봉투를
 * 씌우고, 봉투는 진행자가 기록 양식에서 손으로 옮겨 적는다.
 *
 * <p><b>깨진 봉투에서 조용히 넘어가지 않는다.</b> 한 참가자가 표에서 빠져도 8명 중
 * 7명이라는 사실을 아무도 눈치채지 못한다. 파일 이름과 필드 이름을 달아 세게 실패한다.
 */

export type AgeBand = "60+" | "40-50";
export type Mode = "minui" | "classic";
export type F9Outcome = "read-not-tapped" | "tapped-unread" | "typed-instead";
export type M7Repeat = "same" | "different";

const AGE_BANDS: readonly string[] = ["60+", "40-50"];
const MODES: readonly string[] = ["minui", "classic"];
const F9_OUTCOMES: readonly string[] = ["read-not-tapped", "tapped-unread", "typed-instead"];
const M7_REPEATS: readonly string[] = ["same", "different"];

/** `frontend/src/instrumentation/TaskRecorder.tsx`의 `TaskRun`과 같은 형. */
export interface TaskRun {
  taskId: string;
  mode: Mode;
  targetMenuId: string;
  startedAt: number;
  endedAt: number | null;
  elapsedMs: number | null;
  taps: number;
  screens: string[];
  offTarget: number;
  completed: boolean;
}

export interface Session {
  participant: string;
  ageBand: AgeBand;
  firstMode: Mode;
  runs: TaskRun[];
  /** `"<taskId>:<mode>"` → 개입 단계. 3이면 진행자가 중단시킨 것이다. */
  interventions: Record<string, number>;
  /** 과제별로 참가자가 쓴 낱말. 발화 순서대로, 원문 그대로. */
  words: Record<string, string[]>;
  f9?: F9Outcome;
  m7Repeat?: M7Repeat;
}

function fail(source: string, field: string, detail: string): never {
  throw new Error(`${source}: ${field} — ${detail}`);
}

function asRecord(value: unknown, source: string, field: string): Record<string, unknown> {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    fail(source, field, "객체여야 합니다.");
  }
  return value as Record<string, unknown>;
}

function asString(value: unknown, source: string, field: string): string {
  if (typeof value !== "string" || value === "") fail(source, field, "비어 있지 않은 문자열이어야 합니다.");
  return value;
}

function asNumber(value: unknown, source: string, field: string): number {
  if (typeof value !== "number" || !Number.isFinite(value)) fail(source, field, "숫자여야 합니다.");
  return value;
}

function asEnum<T extends string>(
  value: unknown,
  allowed: readonly string[],
  source: string,
  field: string,
): T {
  const text = asString(value, source, field);
  if (!allowed.includes(text)) fail(source, field, `${allowed.join(" | ")} 중 하나여야 합니다. 받은 값: ${text}`);
  return text as T;
}

function parseRun(raw: unknown, source: string, index: number): TaskRun {
  const field = `runs[${index}]`;
  const run = asRecord(raw, source, field);
  const endedAt = run.endedAt === null ? null : asNumber(run.endedAt, source, `${field}.endedAt`);
  const elapsedMs = run.elapsedMs === null ? null : asNumber(run.elapsedMs, source, `${field}.elapsedMs`);
  const screens = run.screens;
  if (!Array.isArray(screens)) fail(source, `${field}.screens`, "배열이어야 합니다.");

  return {
    taskId: asString(run.taskId, source, `${field}.taskId`),
    mode: asEnum<Mode>(run.mode, MODES, source, `${field}.mode`),
    targetMenuId: asString(run.targetMenuId, source, `${field}.targetMenuId`),
    startedAt: asNumber(run.startedAt, source, `${field}.startedAt`),
    endedAt,
    elapsedMs,
    taps: asNumber(run.taps, source, `${field}.taps`),
    screens: screens.map((screen, at) => asString(screen, source, `${field}.screens[${at}]`)),
    offTarget: asNumber(run.offTarget, source, `${field}.offTarget`),
    completed: run.completed === true,
  };
}

export function parseSession(raw: unknown, source: string): Session {
  const envelope = asRecord(raw, source, "세션");

  const runs = envelope.runs;
  if (!Array.isArray(runs)) fail(source, "runs", "배열이어야 합니다. minuiMetrics.toJSON()의 결과를 그대로 넣으세요.");

  const interventions: Record<string, number> = {};
  for (const [key, value] of Object.entries(
    envelope.interventions === undefined ? {} : asRecord(envelope.interventions, source, "interventions"),
  )) {
    interventions[key] = asNumber(value, source, `interventions.${key}`);
  }

  const words: Record<string, string[]> = {};
  for (const [key, value] of Object.entries(
    envelope.words === undefined ? {} : asRecord(envelope.words, source, "words"),
  )) {
    if (!Array.isArray(value)) fail(source, `words.${key}`, "배열이어야 합니다.");
    words[key] = value.map((word, at) => asString(word, source, `words.${key}[${at}]`));
  }

  return {
    participant: asString(envelope.participant, source, "participant"),
    ageBand: asEnum<AgeBand>(envelope.ageBand, AGE_BANDS, source, "ageBand"),
    firstMode: asEnum<Mode>(envelope.firstMode, MODES, source, "firstMode"),
    runs: runs.map((run, index) => parseRun(run, source, index)),
    interventions,
    words,
    ...(envelope.f9 === undefined
      ? {}
      : { f9: asEnum<F9Outcome>(envelope.f9, F9_OUTCOMES, source, "f9") }),
    ...(envelope.m7Repeat === undefined
      ? {}
      : { m7Repeat: asEnum<M7Repeat>(envelope.m7Repeat, M7_REPEATS, source, "m7Repeat") }),
  };
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
pnpm vitest run --project tools test/usertest-session.test.ts
pnpm --filter tools typecheck
```

기대: 7 passed · typecheck 오류 없음

- [ ] **Step 5: 커밋한다**

```bash
git add tools/src/usertest-session.ts tools/test/usertest-session.test.ts
git commit -m "$(cat <<'EOF'
세션 봉투가 깨졌으면 조용히 넘어가지 않는다

TaskRun[]에는 참가자도 순서도 개입 횟수도 없어서 봉투를 씌웠다. 봉투를 손으로
적는 이상 오타가 난다. 조용히 건너뛰면 8명 중 7명이라는 사실을 아무도 모른다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 지표 집계

**Files:**
- Create: `tools/src/usertest-report.ts`
- Test: `tools/test/usertest-report.test.ts`

**Interfaces:**
- Consumes: Task 1의 `Session`, `AgeBand`, `Mode`, `F9Outcome`
- Produces:
  - `median(values: number[]): number | null`
  - `buildReport(sessions: Session[]): Report`
  - `Report`, `TaskStat`, `ModeStat`, `OrderEffect` 타입
  - 상수 `PRIMARY_TASKS: readonly ["T1", "T2", "T3"]`

**집계 규칙 (여기가 이 도구의 설계다):**

1. `T2-repeat`와 `F9`는 1차 지표에서 뺀다 — 앞은 학습 효과가 실렸고, 뒤는 시간이 아니라 세 갈래를 보는 과제다
2. **완료 시간은 완료한 수행만.** 미완료는 완료율에만 든다. 중단(개입 3)한 과제의 시각은 진행자가 60초를 셋 셌다는 사실이지 참가자의 수행 시간이 아니다
3. **개입 1·2를 받고 완료한 수행 수를 따로 센다.** 그 시간에는 힌트가 실려 있으므로 숨기지 않고 옆에 세운다
4. 순서 효과는 각 참가자의 `firstMode`로 가른다 — counterbalancing이 8명에서 상쇄된다는 보장이 없으니 눈으로 본다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tools/test/usertest-report.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import type { Mode, Session, TaskRun } from "../src/usertest-session.js";
import { buildReport, median } from "../src/usertest-report.js";

function run(taskId: string, mode: Mode, elapsedMs: number, completed = true): TaskRun {
  return {
    taskId,
    mode,
    targetMenuId: "inquiry.balance",
    startedAt: 0,
    endedAt: elapsedMs,
    elapsedMs,
    taps: 1,
    screens: [],
    offTarget: 0,
    completed,
  };
}

function session(overrides: Partial<Session> = {}): Session {
  return {
    participant: "P01",
    ageBand: "60+",
    firstMode: "minui",
    runs: [],
    interventions: {},
    words: {},
    ...overrides,
  };
}

describe("median", () => {
  it("표본이 없으면 null이다 — 0이 아니다", () => {
    expect(median([])).toBeNull();
  });

  it("홀수 개는 가운데 값이다", () => {
    expect(median([30, 10, 20])).toBe(20);
  });

  it("짝수 개는 가운데 둘의 평균이다", () => {
    expect(median([40, 10, 20, 30])).toBe(25);
  });
});

describe("buildReport", () => {
  it("참가자 수를 연령대별로 센다", () => {
    const report = buildReport([
      session({ participant: "P01", ageBand: "60+" }),
      session({ participant: "P02", ageBand: "40-50" }),
      session({ participant: "P03", ageBand: "60+" }),
    ]);

    expect(report.participants).toBe(3);
    expect(report.byAgeBand["60+"]).toBe(2);
    expect(report.byAgeBand["40-50"]).toBe(1);
  });

  it("완료 시간은 완료한 수행만 센다", () => {
    const report = buildReport([
      session({ runs: [run("T1", "minui", 10_000), run("T1", "minui", 900_000, false)] }),
    ]);

    const t1 = report.tasks.find((task) => task.taskId === "T1")!;
    expect(t1.minui.samplesMs).toEqual([10_000]);
    expect(t1.minui.medianMs).toBe(10_000);
    expect(t1.minui.attempted).toBe(2);
    expect(t1.minui.completed).toBe(1);
  });

  // 중단은 미완료이므로 규칙 2가 이미 시간을 뺀다. 여기서는 중단 수를 따로 세는지 본다.
  it("진행자가 중단시킨 수행을 따로 센다", () => {
    const report = buildReport([
      session({
        runs: [run("T2", "classic", 180_000, false)],
        interventions: { "T2:classic": 3 },
      }),
    ]);

    const t2 = report.tasks.find((task) => task.taskId === "T2")!;
    expect(t2.classic.aborted).toBe(1);
    expect(t2.classic.samplesMs).toEqual([]);
    expect(t2.classic.medianMs).toBeNull();
  });

  it("개입을 받고 완료한 수행을 따로 센다 — 그 시간에는 힌트가 실려 있다", () => {
    const report = buildReport([
      session({ runs: [run("T3", "minui", 50_000)], interventions: { "T3:minui": 2 } }),
      session({ participant: "P02", runs: [run("T3", "minui", 30_000)] }),
    ]);

    const t3 = report.tasks.find((task) => task.taskId === "T3")!;
    expect(t3.minui.assisted).toBe(1);
    expect(t3.minui.samplesMs).toEqual([30_000, 50_000]);
  });

  it("T2-repeat와 F9는 1차 지표에서 뺀다", () => {
    const report = buildReport([
      session({
        runs: [run("T2", "minui", 20_000), run("T2-repeat", "minui", 5_000), run("F9", "minui", 9_000)],
      }),
    ]);

    expect(report.tasks.map((task) => task.taskId)).toEqual(["T1", "T2", "T3"]);
    const t2 = report.tasks.find((task) => task.taskId === "T2")!;
    expect(t2.minui.samplesMs).toEqual([20_000]);
  });

  it("오탐색률은 수행당 평균이다", () => {
    const first = run("T2", "classic", 20_000);
    first.offTarget = 3;
    const second = run("T2", "classic", 20_000);
    second.offTarget = 1;

    const report = buildReport([session({ runs: [first, second] })]);

    expect(report.tasks.find((task) => task.taskId === "T2")!.classic.offTargetPerRun).toBe(2);
  });

  it("완료율은 모드별로 모든 과제를 합쳐 센다", () => {
    const report = buildReport([
      session({
        runs: [
          run("T1", "minui", 10_000),
          run("T2", "minui", 10_000, false),
          run("T3", "minui", 10_000),
          run("T1", "classic", 10_000),
        ],
      }),
    ]);

    expect(report.completion.minui).toEqual({ completed: 2, attempted: 3 });
    expect(report.completion.classic).toEqual({ completed: 1, attempted: 1 });
  });

  it("순서 효과를 참가자의 firstMode로 가른다", () => {
    const report = buildReport([
      // 쉬운 모드를 먼저 한 사람 — minui가 '먼저', classic이 '나중'
      session({ participant: "P01", firstMode: "minui", runs: [run("T1", "minui", 60_000), run("T1", "classic", 20_000)] }),
      // 기본 UI를 먼저 한 사람 — classic이 '먼저', minui가 '나중'
      session({ participant: "P02", firstMode: "classic", runs: [run("T1", "classic", 40_000), run("T1", "minui", 10_000)] }),
    ]);

    expect(report.order.firstMedianMs).toBe(50_000);
    expect(report.order.secondMedianMs).toBe(15_000);
  });

  it("F9 세 갈래와 M7 반복을 센다", () => {
    const report = buildReport([
      session({ participant: "P01", f9: "read-not-tapped", m7Repeat: "same" }),
      session({ participant: "P02", f9: "read-not-tapped", m7Repeat: "different" }),
      session({ participant: "P03", f9: "tapped-unread" }),
    ]);

    expect(report.f9).toEqual({ "read-not-tapped": 2, "tapped-unread": 1, "typed-instead": 0 });
    expect(report.m7).toEqual({ same: 1, different: 1 });
  });

  it("세션이 하나도 없으면 전부 빈 채로 돌려준다 — 0을 지어내지 않는다", () => {
    const report = buildReport([]);

    expect(report.participants).toBe(0);
    expect(report.tasks.every((task) => task.minui.medianMs === null)).toBe(true);
    expect(report.order.firstMedianMs).toBeNull();
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
pnpm vitest run --project tools test/usertest-report.test.ts
```

기대: FAIL — `Failed to resolve import "../src/usertest-report.js"`

- [ ] **Step 3: 최소 구현을 쓴다**

`tools/src/usertest-report.ts`:

```ts
import type { AgeBand, F9Outcome, Mode, Session, TaskRun } from "./usertest-session.js";

/**
 * M10 1차 지표 집계 (기획안 §12.1 · §12.9 · §12.10).
 *
 * <p>파일 시스템을 모른다. 계산에 입출력이 붙으면 규칙을 테스트할 수 없고,
 * 규칙을 테스트할 수 없으면 "중단한 과제를 뺐는가" 같은 것을 사람이 눈으로 확인하게 된다.
 *
 * <p>여기서 하지 않는 것: <b>유의성 판정.</b> 표본 8명에 검정력이 없다. p값을 계산하는
 * 순간 이 표를 읽는 사람이 그것을 근거로 쓴다.
 */

/** 1차 지표에 드는 과제. `T2-repeat`(학습 효과)와 `F9`(시간이 아니라 세 갈래)는 빠진다. */
export const PRIMARY_TASKS = ["T1", "T2", "T3"] as const;

const F9_OUTCOMES = ["read-not-tapped", "tapped-unread", "typed-instead"] as const;

export interface ModeStat {
  /** 완료한 수행만. 표본이 없으면 null — 0이 아니다. */
  medianMs: number | null;
  /** 중앙값의 원자료, 오름차순. 8명에서 평균은 한 명이 흔든다 — 독자가 직접 본다. */
  samplesMs: number[];
  completed: number;
  attempted: number;
  /** 개입 1·2를 받고 완료한 수. 그 시간에는 진행자의 힌트가 실려 있다. */
  assisted: number;
  /** 개입 3 — 진행자가 중단시킨 수. */
  aborted: number;
  offTargetPerRun: number | null;
}

export interface TaskStat {
  taskId: string;
  minui: ModeStat;
  classic: ModeStat;
}

export interface OrderEffect {
  /** 참가자가 **먼저** 한 모드의 완료 시간 중앙값. */
  firstMedianMs: number | null;
  /** **나중에** 한 모드의 중앙값. 둘이 크게 갈리면 counterbalancing이 상쇄하지 못한 것이다. */
  secondMedianMs: number | null;
}

export interface Report {
  participants: number;
  byAgeBand: Record<AgeBand, number>;
  tasks: TaskStat[];
  completion: Record<Mode, { completed: number; attempted: number }>;
  order: OrderEffect;
  f9: Record<F9Outcome, number>;
  m7: { same: number; different: number };
}

export function median(values: number[]): number | null {
  if (values.length === 0) return null;
  const sorted = [...values].sort((a, b) => a - b);
  const middle = Math.floor(sorted.length / 2);
  return sorted.length % 2 === 1
    ? sorted[middle]!
    : (sorted[middle - 1]! + sorted[middle]!) / 2;
}

/** 진행자가 그 수행에 개입한 단계. 기록이 없으면 0. */
function interventionOf(session: Session, run: TaskRun): number {
  return session.interventions[`${run.taskId}:${run.mode}`] ?? 0;
}

function statOf(pairs: { session: Session; run: TaskRun }[]): ModeStat {
  const samplesMs = pairs
    .filter(({ run }) => run.completed && run.elapsedMs !== null)
    .map(({ run }) => run.elapsedMs!)
    .sort((a, b) => a - b);

  const offTargets = pairs.map(({ run }) => run.offTarget);

  return {
    medianMs: median(samplesMs),
    samplesMs,
    completed: pairs.filter(({ run }) => run.completed).length,
    attempted: pairs.length,
    assisted: pairs.filter(
      ({ session, run }) => run.completed && interventionOf(session, run) > 0,
    ).length,
    aborted: pairs.filter(({ session, run }) => interventionOf(session, run) >= 3).length,
    offTargetPerRun:
      offTargets.length === 0
        ? null
        : offTargets.reduce((sum, value) => sum + value, 0) / offTargets.length,
  };
}

export function buildReport(sessions: Session[]): Report {
  const primary = new Set<string>(PRIMARY_TASKS);

  const pairs = sessions.flatMap((session) =>
    session.runs
      .filter((run) => primary.has(run.taskId))
      .map((run) => ({ session, run })),
  );

  const tasks: TaskStat[] = PRIMARY_TASKS.map((taskId) => ({
    taskId,
    minui: statOf(pairs.filter((pair) => pair.run.taskId === taskId && pair.run.mode === "minui")),
    classic: statOf(pairs.filter((pair) => pair.run.taskId === taskId && pair.run.mode === "classic")),
  }));

  const completion = {
    minui: { completed: 0, attempted: 0 },
    classic: { completed: 0, attempted: 0 },
  } satisfies Record<Mode, { completed: number; attempted: number }>;
  for (const { run } of pairs) {
    completion[run.mode].attempted += 1;
    if (run.completed) completion[run.mode].completed += 1;
  }

  const done = pairs.filter(({ run }) => run.completed && run.elapsedMs !== null);
  const order: OrderEffect = {
    firstMedianMs: median(
      done.filter(({ session, run }) => run.mode === session.firstMode).map(({ run }) => run.elapsedMs!),
    ),
    secondMedianMs: median(
      done.filter(({ session, run }) => run.mode !== session.firstMode).map(({ run }) => run.elapsedMs!),
    ),
  };

  const byAgeBand: Record<AgeBand, number> = { "60+": 0, "40-50": 0 };
  for (const session of sessions) byAgeBand[session.ageBand] += 1;

  const f9 = Object.fromEntries(F9_OUTCOMES.map((outcome) => [outcome, 0])) as Record<F9Outcome, number>;
  for (const session of sessions) if (session.f9) f9[session.f9] += 1;

  const m7 = { same: 0, different: 0 };
  for (const session of sessions) if (session.m7Repeat) m7[session.m7Repeat] += 1;

  return { participants: sessions.length, byAgeBand, tasks, completion, order, f9, m7 };
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
pnpm vitest run --project tools test/usertest-report.test.ts
pnpm --filter tools typecheck
```

기대: 13 passed (median 3 · buildReport 10) · typecheck 오류 없음

- [ ] **Step 5: 커밋한다**

```bash
git add tools/src/usertest-report.ts tools/test/usertest-report.test.ts
git commit -m "$(cat <<'EOF'
완료 시간이 진행자의 인내심을 재지 않게 한다

중단한 과제의 시각은 참가자의 수행 시간이 아니라 진행자가 60초를 셋 셌다는 사실이다.
완료한 수행만 시간에 넣고, 개입을 받고 완료한 수는 숨기지 않고 옆에 세운다.

8명에서 평균은 한 명이 흔들므로 중앙값과 원자료를 함께 낸다. 유의성은 계산하지 않는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: CLI와 세션 디렉터리

**Files:**
- Create: `tools/src/report-usertest.ts`, `tools/sessions/EXAMPLE.json`
- Modify: `tools/package.json`, `.gitignore`

**Interfaces:**
- Consumes: Task 1의 `parseSession`, Task 2의 `buildReport`·`Report`
- Produces: `pnpm --filter tools report:usertest` 명령

- [ ] **Step 1: `.gitignore`에 세션 디렉터리를 넣는다**

`.gitignore`의 `# 측정 산출물` 블록 아래에 붙인다:

```gitignore
# 사용자 테스트 세션 기록 (M10). 실제 참가자의 것이므로 저장소에 올리지 않는다.
# 형식은 tools/sessions/EXAMPLE.json 하나만 남긴다.
tools/sessions/*.json
!tools/sessions/EXAMPLE.json
```

- [ ] **Step 2: 형식 예시를 쓴다**

`tools/sessions/EXAMPLE.json`:

```json
{
  "participant": "P01",
  "ageBand": "60+",
  "firstMode": "minui",
  "runs": [
    {
      "taskId": "T1",
      "mode": "minui",
      "targetMenuId": "inquiry.balance",
      "startedAt": 1755300000000,
      "endedAt": 1755300012000,
      "elapsedMs": 12000,
      "taps": 1,
      "screens": ["inquiry.balance"],
      "offTarget": 0,
      "completed": true
    },
    {
      "taskId": "T2",
      "mode": "classic",
      "targetMenuId": "transfer.auto",
      "startedAt": 1755300100000,
      "endedAt": 1755300221000,
      "elapsedMs": 121000,
      "taps": 9,
      "screens": ["product.savings", "settings.alarm", "transfer.auto"],
      "offTarget": 2,
      "completed": true
    }
  ],
  "interventions": { "T2:classic": 2 },
  "words": { "T2": ["자동으로 나가는 거", "그 매달 빠지는 거"] },
  "f9": "read-not-tapped",
  "m7Repeat": "same"
}
```

- [ ] **Step 3: CLI를 쓴다**

`tools/src/report-usertest.ts`:

```ts
import { existsSync, readFileSync, readdirSync } from "node:fs";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
import { buildReport, type ModeStat, type Report } from "./usertest-report.js";
import { parseSession, type Session } from "./usertest-session.js";

/**
 * M10 사용자 테스트 리포트 (기획안 §12.9 · §12.10).
 *
 * <p>`report-metrics.ts`가 "사람이 있어야 나온다"며 비워 둔 세 칸 — 완료 시간·완료율·
 * 오탐색률 — 을 채우는 짝이다. <b>세션 파일이 없으면 그 칸을 그대로 비워 둔다.</b>
 * 목표를 맞추려고 기준을 옮기지 않는 것과 같은 규칙이다.
 *
 * <p>진행자는 세션마다 `copy(minuiMetrics.toJSON())`으로 회수한 것을 봉투에 넣어
 * `tools/sessions/P01.json`으로 저장한다. 형식은 `tools/sessions/EXAMPLE.json`.
 */

const HERE = dirname(fileURLToPath(import.meta.url));
const SESSIONS_DIR = join(HERE, "../sessions");

/** 형식 예시는 집계에 넣지 않는다 — 넣으면 없는 참가자가 표에 선다. */
const EXAMPLE = "EXAMPLE.json";

/** 표에서 과제 번호만 보이면 무엇이었는지 기억해야 한다. 이름을 붙여 둔다. */
const TASK_LABELS: Record<string, string> = {
  T1: "T1 잔액 (자주 쓰는 것)",
  T2: "T2 자동이체 (찾아야 하는 것)",
  T3: "T3 이체 한도 (이름을 모르는 것)",
};

function loadSessions(): Session[] {
  if (!existsSync(SESSIONS_DIR)) return [];
  return readdirSync(SESSIONS_DIR)
    .filter((name) => name.endsWith(".json") && name !== EXAMPLE)
    .sort()
    .map((name) => parseSession(JSON.parse(readFileSync(join(SESSIONS_DIR, name), "utf8")), name));
}

const seconds = (ms: number | null) => (ms === null ? "—" : `${(ms / 1000).toFixed(0)}s`);
const sampleList = (samplesMs: number[]) =>
  samplesMs.length === 0 ? "—" : `[${samplesMs.map((ms) => (ms / 1000).toFixed(0)).join(",")}]`;

function diff(a: number | null, b: number | null): string {
  if (a === null || b === null) return "—";
  if (b === 0) return a === 0 ? "동률" : "+∞";
  return `${(((a - b) / b) * 100).toFixed(0)}%`;
}

function note(stat: ModeStat): string {
  const parts: string[] = [];
  if (stat.assisted > 0) parts.push(`개입 후 완료 ${stat.assisted}`);
  if (stat.aborted > 0) parts.push(`중단 ${stat.aborted}`);
  return parts.length === 0 ? "" : `  (${parts.join(" · ")})`;
}

function print(report: Report): void {
  console.log(
    `\n═══ M10 1차 지표 — 참가자 ${report.participants}명 ` +
      `(60+ ${report.byAgeBand["60+"]}, 40-50 ${report.byAgeBand["40-50"]}) ═══\n`,
  );

  console.log("완료 시간 (중앙값)                   쉬운 모드    기본 UI      차이");
  for (const task of report.tasks) {
    console.log(
      `  ${(TASK_LABELS[task.taskId] ?? task.taskId).padEnd(33)}${seconds(task.minui.medianMs).padStart(9)}` +
        `${seconds(task.classic.medianMs).padStart(11)}` +
        `${diff(task.minui.medianMs, task.classic.medianMs).padStart(10)}`,
    );
    console.log(
      `     ${sampleList(task.minui.samplesMs)}  ${sampleList(task.classic.samplesMs)}` +
        `${note(task.minui)}${note(task.classic)}`,
    );
  }

  const rate = (part: { completed: number; attempted: number }) =>
    part.attempted === 0
      ? "—"
      : `${part.completed}/${part.attempted} (${((part.completed / part.attempted) * 100).toFixed(0)}%)`;

  console.log(
    `\n  ${"완료율".padEnd(33)}${rate(report.completion.minui).padStart(9)}` +
      `${rate(report.completion.classic).padStart(11)}`,
  );

  const off = (value: number | null) => (value === null ? "—" : value.toFixed(1));
  const offAverage = (mode: "minui" | "classic") => {
    const values = report.tasks.map((task) => task[mode].offTargetPerRun).filter((v): v is number => v !== null);
    return values.length === 0 ? null : values.reduce((sum, v) => sum + v, 0) / values.length;
  };
  console.log(
    `  ${"오탐색률 (수행당)".padEnd(33)}${off(offAverage("minui")).padStart(9)}` +
      `${off(offAverage("classic")).padStart(11)}`,
  );

  const { firstMedianMs, secondMedianMs } = report.order;
  const skewed =
    firstMedianMs !== null && secondMedianMs !== null && secondMedianMs < firstMedianMs * 0.7;
  console.log(
    `\n순서 효과 — 먼저 한 모드 ${seconds(firstMedianMs)} / 나중 한 모드 ${seconds(secondMedianMs)}` +
      (skewed ? "  ⚠ 상쇄 안 됨 — 그대로 보고할 것" : ""),
  );

  console.log(
    `\nF9  읽고 안 누름 ${report.f9["read-not-tapped"]} · ` +
      `안 읽고 누름 ${report.f9["tapped-unread"]} · 직접 침 ${report.f9["typed-instead"]}`,
  );
  console.log(`M7  같은 말 ${report.m7.same} / 다른 말 ${report.m7.different}`);

  console.log(
    `\n표본 ${report.participants}명 — 유의성 주장 없음. 관찰된 패턴과 개별 사례로 읽을 것.\n`,
  );
}

const sessions = loadSessions();

if (sessions.length === 0) {
  console.log("\n═══ M10 1차 지표 ═══\n");
  console.log("  완료 시간 · 완료율 · 오탐색률 — 미측정 (사람이 있어야 나온다)\n");
  console.log(`  세션 기록을 ${SESSIONS_DIR}에 P01.json 형태로 두면 여기 채워집니다.`);
  console.log("  형식: tools/sessions/EXAMPLE.json · 진행 절차: docs/사용자테스트-키트.md\n");
} else {
  print(buildReport(sessions));
}
```

- [ ] **Step 4: `tools/package.json`에 스크립트를 넣는다**

`"report": "tsx src/report-metrics.ts",` 다음 줄에 붙인다:

```json
    "report:usertest": "tsx src/report-usertest.ts",
```

- [ ] **Step 5: 세션이 없을 때의 출력을 확인한다**

```bash
pnpm --filter tools report:usertest
```

기대: "완료 시간 · 완료율 · 오탐색률 — 미측정 (사람이 있어야 나온다)"이 찍히고 정상 종료.
**숫자가 하나라도 찍히면 잘못된 것이다** — `EXAMPLE.json`이 집계에 섞였다는 뜻이다.

- [ ] **Step 6: 예시 파일로 표 출력을 확인한다**

```bash
cp tools/sessions/EXAMPLE.json tools/sessions/P99.json
pnpm --filter tools report:usertest
rm tools/sessions/P99.json
```

기대: 참가자 1명 표가 찍힌다. T1 쉬운 모드 12s, T2 기본 UI 121s, T2 줄에 `(개입 후 완료 1)`.

- [ ] **Step 7: 깨진 봉투에서 세게 실패하는지 확인한다**

```bash
echo '{"participant":"P98","ageBand":"60대","firstMode":"minui","runs":[]}' > tools/sessions/P98.json
pnpm --filter tools report:usertest; echo "종료 코드: $?"
rm tools/sessions/P98.json
```

기대: `P98.json: ageBand — ...` 오류와 함께 0이 아닌 종료 코드.

- [ ] **Step 8: 커밋한다**

```bash
git add tools/src/report-usertest.ts tools/sessions/EXAMPLE.json tools/package.json .gitignore
git commit -m "$(cat <<'EOF'
비워 둔 세 칸을 채우는 짝을 만든다

report-metrics.ts가 "사람이 있어야 나온다"고 비워 둔 완료 시간·완료율·오탐색률을
세션 기록에서 집계한다. 세션이 없으면 그 칸을 그대로 비워 둔다.

참가자 기록은 저장소에 올리지 않는다. 형식 예시 하나만 남긴다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 음성 덮어쓰기 provider

**Files:**
- Create: `frontend/src/instrumentation/ScriptedOverrideStt.ts`
- Test: `frontend/test/sttOverride.test.ts`

**Interfaces:**
- Consumes: `@minui/react`의 `SttLike`, `@minui/voice`의 `MockSttProvider` (테스트에서만)
- Produces: `class ScriptedOverrideStt implements SttLike` — 생성자 `(inner: SttLike)`, 메서드 `next(text: string): void`, `clear(): void`

**왜 통째로 바꾸지 않는가:** `MockSttProvider`로 교체하면 F9 과제만 따로 열어야 하고, 새로 열면 `minuiMetrics`가 메모리에 들고 있던 그때까지의 기록이 날아간다. 감싸면 마이크가 그대로 열리고 최종 전사 한 번만 갈린다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`frontend/test/sttOverride.test.ts`:

```ts
import { MockSttProvider } from "@minui/voice";
import { describe, expect, it } from "vitest";
import { ScriptedOverrideStt } from "../src/instrumentation/ScriptedOverrideStt.js";

function harness(script: NonNullable<ConstructorParameters<typeof MockSttProvider>[0]>) {
  const inner = new MockSttProvider(script);
  const stt = new ScriptedOverrideStt(inner);
  const finals: string[] = [];
  const partials: string[] = [];
  const errors: string[] = [];
  stt.onFinal((result) => finals.push(result.text));
  stt.onPartial((text) => partials.push(text));
  stt.onError((error) => errors.push(error.code));
  return { stt, finals, partials, errors };
}

describe("ScriptedOverrideStt", () => {
  it("넣어 둔 것이 없으면 실제 인식 결과가 그대로 나온다", async () => {
    const { stt, finals } = harness([{ text: "잔액 얼마야" }]);

    await stt.start();

    expect(finals).toEqual(["잔액 얼마야"]);
  });

  it("넣어 두면 최종 전사가 그것으로 바뀐다", async () => {
    const { stt, finals } = harness([{ text: "우리 딸한테 삼십만원 보내줘" }]);

    stt.next("우리 딸한테 십삼만원 보내줘");
    await stt.start();

    expect(finals).toEqual(["우리 딸한테 십삼만원 보내줘"]);
  });

  // 한 번 쓰고 남아 있으면 다음 과제까지 오염된다.
  it("한 번 쓰면 소진된다", async () => {
    const { stt, finals } = harness([{ text: "첫 번째" }, { text: "두 번째" }]);

    stt.next("넣어 둔 것");
    await stt.start();
    await stt.start();

    expect(finals).toEqual(["넣어 둔 것", "두 번째"]);
  });

  // 고령 발화에서 STT가 흔들려도 F9 프로토콜은 굴러가야 한다.
  it("실제 인식이 실패해도 넣어 둔 것이 최종 결과로 나간다", async () => {
    const { stt, finals, errors } = harness([
      { text: "", error: { code: "no-speech", message: "못 들었습니다." } },
    ]);

    stt.next("우리 딸한테 십삼만원 보내줘");
    await stt.start();

    expect(finals).toEqual(["우리 딸한테 십삼만원 보내줘"]);
    expect(errors).toEqual([]);
  });

  it("넣어 둔 것이 없으면 오류는 오류대로 간다", async () => {
    const { stt, finals, errors } = harness([
      { text: "", error: { code: "no-speech", message: "못 들었습니다." } },
    ]);

    await stt.start();

    expect(finals).toEqual([]);
    expect(errors).toEqual(["no-speech"]);
  });

  // 참가자는 자기가 한 말을 화면에서 봐야 한다. 중간 결과까지 갈면 조작이 드러난다.
  it("중간 결과는 언제나 실제 그대로다", async () => {
    const { stt, partials } = harness([
      { partials: ["우리 딸", "우리 딸한테 삼십만원"], text: "우리 딸한테 삼십만원 보내줘" },
    ]);

    stt.next("우리 딸한테 십삼만원 보내줘");
    await stt.start();

    expect(partials).toEqual(["우리 딸", "우리 딸한테 삼십만원"]);
  });

  it("clear로 넣어 둔 것을 되돌린다", async () => {
    const { stt, finals } = harness([{ text: "실제 결과" }]);

    stt.next("넣어 둔 것");
    stt.clear();
    await stt.start();

    expect(finals).toEqual(["실제 결과"]);
  });

  it("감싼 provider의 지원 여부를 그대로 물려받는다", () => {
    const { stt } = harness([]);

    expect(stt.isSupported).toBe(true);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
pnpm vitest run --project frontend test/sttOverride.test.ts
```

기대: FAIL — `Failed to resolve import "../src/instrumentation/ScriptedOverrideStt.js"`

- [ ] **Step 3: 최소 구현을 쓴다**

`frontend/src/instrumentation/ScriptedOverrideStt.ts`:

```ts
import type { SttLike } from "@minui/react";

/**
 * 진행자가 최종 전사를 한 번 갈아끼울 수 있게 실제 STT를 감싼다 (기획안 §12.10, F9 프로토콜).
 *
 * <p>F9가 물으려는 것은 "참가자가 <b>말한 금액과 다른 금액</b>이 제안으로 떴을 때 잡아내는가"다.
 * 실제 마이크로는 그 상황을 만들 수 없고, 우연히 오인식이 나기를 기다릴 수도 없다.
 *
 * <p><b>통째로 바꾸지 않고 감싼다.</b> `MockSttProvider`로 교체하면 F9 과제만 따로 열어야
 * 하는데, 새로 열면 `minuiMetrics`가 메모리에 들고 있던 그때까지의 기록이 날아간다.
 * 감싸면 마이크는 그대로 열리고, 참가자는 평소처럼 말하고, 중간 결과로 자기 말을 본다.
 * 갈리는 것은 최종 전사 한 번뿐이다.
 *
 * <p>실제 인식이 <b>실패해도</b> 넣어 둔 문장을 내보낸다. 고령 발화에서 STT가 흔들리는 것은
 * 흔한 일이고, 그때마다 프로토콜이 무너지면 F9를 잴 기회가 사라진다.
 */
export class ScriptedOverrideStt implements SttLike {
  readonly #inner: SttLike;
  readonly #partial = new Set<(text: string) => void>();
  readonly #final = new Set<(result: { text: string; confidence: number }) => void>();
  readonly #error = new Set<(error: { code: string; message: string }) => void>();

  #queued: string | null = null;

  /** 감싼 엔진이 끝을 알려 줘야 하는 종류일 때만 생긴다 (`SttLike.finish`). */
  finish?: () => void | Promise<void>;

  constructor(inner: SttLike) {
    this.#inner = inner;

    inner.onPartial((text) => {
      for (const listener of this.#partial) listener(text);
    });

    inner.onFinal((result) => {
      const override = this.#take();
      this.#emitFinal(override === null ? result : { text: override, confidence: 1 });
    });

    inner.onError((error) => {
      const override = this.#take();
      if (override === null) {
        for (const listener of this.#error) listener(error);
        return;
      }
      this.#emitFinal({ text: override, confidence: 1 });
    });

    if (inner.finish) this.finish = () => inner.finish!();
  }

  get isSupported(): boolean {
    return this.#inner.isSupported;
  }

  /** 다음 최종 전사를 이 문장으로 바꾼다. 한 번 쓰면 소진된다. */
  next(text: string): void {
    this.#queued = text;
  }

  /** 넣어 둔 것을 되돌린다. */
  clear(): void {
    this.#queued = null;
  }

  start(): Promise<void> {
    return this.#inner.start();
  }

  stop(): void {
    this.#inner.stop();
  }

  onPartial(callback: (text: string) => void): () => void {
    this.#partial.add(callback);
    return () => this.#partial.delete(callback);
  }

  onFinal(callback: (result: { text: string; confidence: number }) => void): () => void {
    this.#final.add(callback);
    return () => this.#final.delete(callback);
  }

  onError(callback: (error: { code: string; message: string }) => void): () => void {
    this.#error.add(callback);
    return () => this.#error.delete(callback);
  }

  #take(): string | null {
    const queued = this.#queued;
    this.#queued = null;
    return queued;
  }

  #emitFinal(result: { text: string; confidence: number }): void {
    for (const listener of this.#final) listener(result);
  }
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
pnpm vitest run --project frontend test/sttOverride.test.ts
pnpm typecheck
```

기대: 8 passed · typecheck 오류 없음

- [ ] **Step 5: 커밋한다**

```bash
git add frontend/src/instrumentation/ScriptedOverrideStt.ts frontend/test/sttOverride.test.ts
git commit -m "$(cat <<'EOF'
말한 금액과 다른 금액을 띄울 수 있게 한다 (F9 프로토콜)

F9가 묻는 것은 제안이 안전 장치로 작동하는가인데, 실제 마이크로는 "틀린 제안"을
만들 수 없다. 실제 STT를 감싸 최종 전사 한 번만 갈아끼운다.

통째로 바꾸지 않은 이유는 그러면 F9 과제만 새로 열어야 하고, 새로 열면 그때까지의
계측 기록이 날아가기 때문이다. 인식이 실패해도 넣어 둔 문장을 내보낸다 — 고령 발화에서
STT가 흔들릴 때마다 프로토콜이 무너지면 잴 기회가 사라진다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 콘솔 손잡이와 배선

**Files:**
- Create: `frontend/src/instrumentation/SttBridge.tsx`
- Modify: `frontend/src/main.tsx`

**Interfaces:**
- Consumes: Task 4의 `ScriptedOverrideStt`, 기존 `frontend/src/stt.ts`의 `makeStt()`
- Produces: `window.minuiStt` — `{ next(text: string): void; clear(): void }`. `?stt=script`로 열었을 때만 생긴다

- [ ] **Step 1: 손잡이를 쓴다**

`frontend/src/instrumentation/SttBridge.tsx` — `MetricsBridge.tsx`와 같은 모양:

```tsx
import { useEffect } from "react";
import type { ScriptedOverrideStt } from "./ScriptedOverrideStt.js";

declare global {
  interface Window {
    /** F9 프로토콜에서 진행자가 잡는 손잡이. `?stt=script`일 때만 있다. */
    minuiStt?: { next: (text: string) => void; clear: () => void };
  }
}

/**
 * 음성 덮어쓰기를 브라우저 콘솔에 노출한다.
 *
 * <p>`MetricsBridge`와 같은 이유로 화면에 아무것도 띄우지 않는다 — 참가자가 평소처럼
 * 앱을 쓰는 모습을 보는 것이 목적인데, 조작 장치가 화면에 있으면 그 자체가 행동을 바꾼다.
 *
 * <p>F9 진행 순서:
 * <pre>
 *   minuiStt.next("우리 딸한테 십삼만원 보내줘")   // 참가자에게 "삼십만원"을 말하게 하기 직전
 *   ... 참가자가 마이크를 누르고 말한다 ...
 *   // 이체 화면의 금액 제안이 13만원으로 뜬다. 참가자가 그것을 잡아내는지 본다.
 * </pre>
 */
export function SttBridge({ stt }: { stt: ScriptedOverrideStt }) {
  useEffect(() => {
    window.minuiStt = { next: (text) => stt.next(text), clear: () => stt.clear() };
    return () => {
      delete window.minuiStt;
    };
  }, [stt]);

  return null;
}
```

- [ ] **Step 2: `main.tsx`를 배선한다**

`frontend/src/main.tsx`의 import 블록에 두 줄을 더한다:

```tsx
import { ScriptedOverrideStt } from "./instrumentation/ScriptedOverrideStt.js";
import { SttBridge } from "./instrumentation/SttBridge.js";
import { makeStt } from "./stt.js";
```

`const api = ...` 아래에 붙인다:

```tsx
/**
 * `?stt=script`일 때만 음성 덮어쓰기를 끼운다 (F9 프로토콜, 기획안 §12.10).
 *
 * 플래그가 없으면 `MinUIShell`이 지금까지처럼 스스로 `makeStt()`를 만든다 —
 * 이 파일이 기본 경로에 손대지 않는다는 뜻이다.
 */
const scripted = new URLSearchParams(window.location.search).get("stt") === "script";
const stt = scripted ? new ScriptedOverrideStt(makeStt()) : null;
```

`render` 안의 `<App api={api} />`를 바꾼다:

```tsx
    <TaskRecorderProvider>
      <MetricsBridge />
      {stt && <SttBridge stt={stt} />}
      <App api={api} {...(stt ? { stt } : {})} />
    </TaskRecorderProvider>
```

- [ ] **Step 3: 전체 테스트와 타입 검사를 돌린다**

```bash
pnpm test
pnpm typecheck
pnpm build
```

기대: 기존 테스트가 전부 그대로 통과(541 + 이번에 더한 것) · 타입 오류 없음 · 빌드 성공.
**기존 테스트가 하나라도 깨지면 기본 경로를 건드린 것이다** — `?stt=script`가 없을 때는 아무것도 달라지면 안 된다.

- [ ] **Step 4: 실제 앱에서 손잡이가 붙는지 확인한다**

```bash
pnpm --filter demos dev
```

브라우저에서 프런트를 `?stt=script` 없이 연 뒤 콘솔에 `minuiStt`를 친다 → `undefined`.
`?stt=script`를 붙여 연 뒤 다시 친다 → `{ next: f, clear: f }`.
(데모 호스트가 아니라 `frontend`를 띄우는 명령이 따로 있으면 그것을 쓴다.)

- [ ] **Step 5: 커밋한다**

```bash
git add frontend/src/instrumentation/SttBridge.tsx frontend/src/main.tsx
git commit -m "$(cat <<'EOF'
진행자만 보이지 않는 곳에서 음성을 갈아끼운다

MetricsBridge와 같은 이유로 화면에 아무것도 띄우지 않는다. 조작 장치가 화면에 있으면
그 자체가 참가자의 행동을 바꾼다.

?stt=script가 없으면 지금과 완전히 같다 — MinUIShell이 스스로 makeStt()를 만든다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 진행 키트 문서

**Files:**
- Create: `docs/사용자테스트-키트.md`

**Interfaces:**
- Consumes: Task 3의 `report:usertest` 명령과 봉투 형식, Task 5의 `minuiStt` 손잡이
- Produces: 진행자가 인쇄해 쓰는 문서. 다른 코드가 이것을 참조하지 않는다

문서는 **인쇄해서 손에 드는 물건**이다. 설명을 길게 쓰지 말고, 세션 중에 눈으로 훑어 찾을 수 있게 쓴다. 근거와 배경은 기획안에 있으므로 여기서 되풀이하지 않고 절 번호로 가리킨다.

- [ ] **Step 1: 문서를 쓴다**

`docs/사용자테스트-키트.md`에 아래 아홉 절을 담는다. 각 절의 **내용은 스펙**
(`docs/superpowers/specs/2026-08-16-m10-user-test-design.md`)에 그대로 있으므로 옮겨 적는다.

1. **세션 전 점검** — 백엔드가 떠 있는가(`docker compose -f backend/compose.yaml up -d`), 프런트를 `?stt=script`로 열었는가, 콘솔에 `minuiMetrics`와 `minuiStt`가 둘 다 있는가, 참가자 번호가 홀수인가 짝수인가(순서가 갈린다), 저장소를 비웠는가(앞 참가자의 학습·사용 기록이 남으면 카드 배치가 달라진다)
2. **동의서** — 화면 녹화·발화 기록, 음성 원본은 분석 후 폐기(§11.2), 화면의 이름·계좌·금액은 데모의 가짜 값, 기록은 P01~P08 번호로만, 언제든 중단 가능하고 불이익 없음. 참가자 서명란
3. **과제 지시문** — 아래 세 문장을 **그대로 읽을 수 있게** 큰 글씨로. 각 지시문 옆에 진행자용 정보(목표 메뉴 ID, `minuiMetrics.begin(...)` 명령 한 줄)를 작게 붙인다. **메뉴 이름을 입에 올리지 않는다** — 참가자가 그 말을 그대로 검색창에 친다

   > **T1** "이번 달 관리비가 빠져나갔는지 궁금하세요. 통장에 얼마 남았는지 보세요."
   > `inquiry.balance` · `minuiMetrics.begin("T1", "inquiry.balance", "minui")`
   >
   > **T2** "매달 자동으로 나가는 전기요금이 있는데, 이제 그만 나가게 하고 싶으세요."
   > `transfer.auto` · `minuiMetrics.begin("T2", "transfer.auto", "classic")`
   >
   > **T3** "은행에서 '한 번에 보낼 수 있는 돈이 막혀 있으니 앱에서 푸세요'라고 했습니다."
   > `settings.limit` · `minuiMetrics.begin("T3", "settings.limit", "minui")`

   세 번째 인자는 그때 참가자가 쓰고 있는 모드로 바꿔 넣는다. 틀리면 두 모드가 한쪽으로 합쳐진다.
4. **진행 순서** — 참가자 홀수: 쉬운 모드로 T1·T2·T3 → 기본 UI로 T1·T2·T3. 짝수는 반대. 그 뒤 F9, 그 뒤 T2 반복(`taskId`는 `T2-repeat`)
5. **개입 기준 표** — 아래 3단 사다리를 그대로 싣는다. 개입 시점이 사람마다 다르면 완료율이 진행자의 인내심을 재게 된다

   | 조건 | 진행자가 하는 말 | 기록 |
   |---|---|---|
   | 60초 무진전 (새 화면 진입 없음) | "지금 무엇을 찾고 계신지 말씀해 주시겠어요?" | 개입 1 |
   | 추가 60초 무진전 | "화면에서 눈에 들어오는 걸 소리 내어 읽어 주세요." | 개입 2 |
   | 추가 60초, 또는 참가자가 포기 의사 | "여기까지 하겠습니다." | 개입 3 (중단·미완료) |

   진행자 금칙 넷: 메뉴 이름 말하기 · "거기 아니에요" · 화면 가리키기 · 참가자가 옳은 방향일 때 고개 끄덕이기.
   think-aloud 유도는 **침묵 15초에 한 번만** — 계속 재촉하면 말하느라 완료 시간이 늘어난다
6. **F9 프로토콜** — `minuiStt.next("우리 딸한테 십삼만원 보내줘")`를 언제 치는가, 참가자에게 "삼십만원"을 말하게 하는 문장, 세 갈래 관찰표와 각각의 기록 값
7. **기록 양식** — 과제당 한 줄. 멈춘 지점 / **쓴 낱말 원문 그대로** / 개입 단계 / 신호 / (F9만) 세 갈래. A4 한 장에 표로
8. **상시 관찰과 사후 인터뷰** — 과제에 묶이지 않는 것들. §12.10의 관찰 항목 중 위 일곱 절이 안 덮는 나머지다

   세션 내내 볼 것 (기록 양식 아래쪽 여백에 적는다):
   - **F3** 카드가 바뀐 것을 알아채는가. 바뀐 이유를 궁금해하는가 → 아니면 변경 고지(배지)를 다시 설계
   - **F8** "왜 이렇게 보이나요?"를 스스로 찾아 들어가는가. 읽고 납득하는가 → 아니면 조용한 링크가 아니라 다른 자리가 필요하다
   - **F7** 뜻풀이가 뜬 자리에서 읽는가, 그냥 지나치는가
   - **§9.2** 되묻기 선택지 3개가 적당한가 — 더 보고 싶어 하는가, 셋도 많아하는가

   사후 인터뷰 네 문항 (그대로 읽는다):
   - "이 화면에 이 넉 장이 있는 게 어떠셨어요?" (F3)
   - "처음 쓸 때 '무엇을 자주 하시나요' 같은 걸 두어 개 물어봤다면 도움이 됐을까요?" (F5)
   - "카드가 하루에 두 장씩 바뀌면 어떠실 것 같아요?" (§8.2 — 시뮬레이션은 `maxSwapsPerRecompute: 2`가 낫다고 말한다)
   - "혹시 오늘 쓰면서 제일 답답했던 데가 어디였어요?"

   **F7의 뜻풀이 채점(상위 30개를 사람이 매기는 것)은 세션과 별개다.** 참가자에게 시키지 않는다 — 과제가 아니라 평가 노동이고, 세션 시간을 다 먹는다. 채점 전까지 "뜻풀이가 정확하다"고 말하지 않는다는 §12.10의 약속은 그대로 남는다
9. **세션 후** — `copy(minuiMetrics.toJSON())`으로 회수 → `tools/sessions/P01.json` 봉투에 넣기(형식은 `tools/sessions/EXAMPLE.json`) → 기록 양식의 손 기록을 `interventions`·`words`·`f9`·`m7Repeat`에 옮겨 적기 → `pnpm --filter tools report:usertest`. 녹음 파일 폐기 확인란

문서 끝에 한 문단으로 못 박는다: **표본 8명에 검정력이 없다. 유의성을 주장하지 말고 관찰된 패턴과 개별 사례로 보고한다. 미달한 지표는 그대로 적는다 — 목표를 맞추려고 기준을 옮기지 않는다.**

- [ ] **Step 2: 문서가 실제 명령과 맞는지 확인한다**

문서에 적힌 명령을 하나씩 실제로 실행한다.

```bash
pnpm --filter tools report:usertest
```

기대: 문서에 적힌 대로 동작한다. **틀린 명령이 적혀 있으면 세션 당일에 진행자가 막힌다** — 이 확인을 건너뛰지 않는다.

- [ ] **Step 3: 커밋한다**

```bash
git add docs/사용자테스트-키트.md
git commit -m "$(cat <<'EOF'
세션 당일에 들고 갈 것을 한 장으로 만든다

지시문에 메뉴 이름을 넣지 않는다 — 넣으면 참가자가 그 말을 그대로 검색창에 치고,
재려던 것(그 기능을 뭐라고 부르는가)이 사라진다.

개입을 60초 3단 사다리로 못 박았다. 개입 시점이 사람마다 다르면 완료율이
진행자의 인내심을 재게 된다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 기획안과 작업 규약 갱신

**Files:**
- Modify: `docs/기획안.md` (§12.10, §13), `AGENTS.md` (명령 목록)

**Interfaces:**
- Consumes: 앞 여섯 태스크의 산출물
- Produces: 없음 (문서가 종점이다)

- [ ] **Step 1: §12.10의 사실 오류를 고친다**

`docs/기획안.md` §12.10 "이미 되어 있는 것"의 세 번째 항목이 지금 이렇게 적혀 있다.

```markdown
- **음성 주입** — `MockSttProvider`에 발화를 넣으면 마이크 없이 같은 경로가 돈다.
  오인식을 **일부러 만들어 넣을 수 있다**
```

이것은 사실이 아니었다 — `MockSttProvider`는 테스트에만 있었고 실행 중인 앱은 진짜 마이크만 썼다. 다음으로 바꾼다.

```markdown
- **음성 덮어쓰기** — `?stt=script`로 열면 진행자가 `minuiStt.next("...")`로 **최종 전사
  한 번**을 갈아끼운다. 마이크는 그대로 열리고 참가자는 자기 말을 중간 결과로 본다.
  F9가 요구하는 "말한 금액과 다른 금액"을 이것으로 만든다
  (`frontend/src/instrumentation/ScriptedOverrideStt.ts`).

  **이 줄은 2026-08-16 전까지 사실이 아니었다.** `MockSttProvider`는 테스트에만 있었고
  실행 중인 앱은 진짜 마이크만 썼다. F9 프로토콜을 준비하다 드러났다
```

- [ ] **Step 2: §12.10 "준비해야 할 것"을 준비된 것으로 옮긴다**

네 항목(과제 지시문·진행 대본·동의서·기록 양식)이 `docs/사용자테스트-키트.md`에 들어갔다.
그 절을 다음으로 바꾼다.

```markdown
#### 준비물 — `docs/사용자테스트-키트.md`

과제 지시문·진행 대본·동의서·기록 양식이 한 장에 있다. 세 가지를 그 문서에서 못 박았다.

- **지시문은 목적으로 적는다.** "자동이체를 해지하세요"라고 말하면 그 말을 그대로
  검색창에 치므로, 재려던 것(**사용자가 그 기능을 뭐라고 부르는가**)이 사라진다.
  "매달 나가는 전기요금을 그만 나가게 해 보세요"처럼 적는다
- **개입은 60초 3단 사다리다.** 개입 시점이 사람마다 다르면 완료율이 진행자의
  인내심을 재게 된다. 3단계는 중단이고 미완료로 기록한다
- **과제는 T1 자주 쓰는 것(`inquiry.balance`) · T2 찾아야 하는 것(`transfer.auto`) ·
  T3 이름을 모르는 것(`settings.limit`)** 셋이다. T1은 §12.4에서 탭 차이가 0이었던
  자리다 — **탭이 0:0인데 완료 시간이 갈리는지**가 "탭 횟수는 난이도의 하한"이라는
  주장의 유일한 시험대다

#### 회수와 집계

`copy(minuiMetrics.toJSON())`으로 회수한 것을 봉투에 넣어 `tools/sessions/P01.json`으로
두고 `pnpm --filter tools report:usertest`를 돌린다. 형식은 `tools/sessions/EXAMPLE.json`.
참가자 기록은 저장소에 올리지 않는다.

집계에서 정한 것 셋:

- **완료 시간은 완료한 수행만 센다.** 중단한 과제의 시각은 참가자의 수행 시간이 아니라
  진행자가 60초를 셋 셌다는 사실이다. 대신 개입을 받고 완료한 수를 옆에 세운다
- **중앙값과 원자료를 함께 낸다.** 8명에서 평균은 한 명이 흔든다
- **순서 효과를 따로 찍는다.** counterbalancing이 8명에서 상쇄된다는 보장이 없으므로
  눈으로 보고, 상쇄가 안 됐으면 그대로 보고한다

도구는 **유의성을 계산하지 않는다.** p값이 표에 있으면 읽는 사람이 그것을 근거로 쓴다.
```

- [ ] **Step 3: §13 로드맵의 M10 줄을 갱신한다**

```markdown
| **M10** 사용자 테스트 | §12.9의 미측정 1차 지표. 키트는 `docs/사용자테스트-키트.md` | 완료 시간·완료율·오탐색률 실측 | 준비 완료 · 참가자 대기 |
```

- [ ] **Step 4: `AGENTS.md`의 명령 목록에 더한다**

`# 측정` 블록의 `pnpm --filter tools measure:portability` 다음 줄에 붙인다:

```bash
pnpm --filter tools report:usertest  # 사용자 테스트 1차 지표 (M10). 세션 없으면 미측정을 그대로 찍는다
```

- [ ] **Step 5: 문서에 적힌 것이 실제와 맞는지 확인한다**

```bash
pnpm --filter tools report:usertest
pnpm test
pnpm typecheck
```

기대: 셋 다 통과. 기획안에 적은 파일 경로(`frontend/src/instrumentation/ScriptedOverrideStt.ts`, `tools/sessions/EXAMPLE.json`, `docs/사용자테스트-키트.md`)가 전부 실재하는지 눈으로 확인한다.

- [ ] **Step 6: 커밋한다**

```bash
git add docs/기획안.md AGENTS.md
git commit -m "$(cat <<'EOF'
사람을 앉히면 그대로 돌아간다고 적을 수 있게 됐다 (M10 준비)

§12.10이 "이미 되어 있다"고 적었던 음성 주입은 사실이 아니었다 — 테스트에만 있었다.
F9 프로토콜을 준비하다 드러났고, 드러난 대로 적었다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## 완료 기준

이 계획이 끝나도 **M10은 끝나지 않는다.** 완료 기준은 완료 시간·완료율·오탐색률의 실측이고,
그것은 참가자 8명이 있어야 나온다. 이 계획이 끝났을 때 참인 것은 이것이다.

- `pnpm test`·`pnpm typecheck`·`pnpm build`가 전부 통과한다
- `?stt=script` 없이 앱을 열면 이 계획 이전과 **동작이 완전히 같다**
- 진행자가 `docs/사용자테스트-키트.md` 한 장으로 세션을 처음부터 끝까지 돌릴 수 있다
- 세션 파일이 없는 상태에서 `report:usertest`가 **"미측정"을 그대로 찍는다**
- §12.1 표의 세 칸은 **여전히 비어 있다.** 사람을 앉히기 전에 채우면 그 표가 거짓이 된다
