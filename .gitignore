node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Gradle / Java
.gradle/
backend/build/
backend/bin/
!backend/gradle/wrapper/gradle-wrapper.jar

# env / local
.env
.env.local
*.local

# API 키. 저장소에 들어가면 안 된다 — 들어간 키는 폐기 말고는 되돌릴 방법이 없다.
api.txt
*.key
secrets/

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# 측정 산출물
tools/out/

# 사용자 테스트 세션 기록 (M10). 실제 참가자의 것이므로 저장소에 올리지 않는다.
# 형식은 tools/sessions/EXAMPLE.json 하나만 남긴다.
tools/sessions/*.json
!tools/sessions/EXAMPLE.json

# Spring Boot / Gradle
backend/.gradle/
backend/build/

# 데모 빌드 산출물
demos/dist/


# 임베딩 모델 (M11). tools의 fetch:model로 다시 받는다.
tools/models/

# 메뉴 벡터 (M11). tools의 build:vectors로 다시 굽는다.
demos/src/vectors/

# 수집한 남의 사이트 본문 (Task 4). 저장소에 넣지 않는 이유는 둘이다 —
# 830KB짜리 남의 글이고, 우리가 배포에 담아 가는 것은 그 전문이 아니라
# 근거로 인용한 문장 몇 줄(shared/host-ai/explain-sources.json)이다.
# 다시 받으려면: pnpm --filter @minui/harvester docs -- https://www.kebhana.com/ kebhana
tools/docs/
